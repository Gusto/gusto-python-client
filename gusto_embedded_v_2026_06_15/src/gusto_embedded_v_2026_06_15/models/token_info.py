"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from gusto_embedded_v_2026_06_15 import models, utils
from gusto_embedded_v_2026_06_15.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import field_serializer, model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class ResourceTypedDict(TypedDict):
    r"""The resource associated with this access token. Null when
    the token has no associated resource.

    """

    type: NotRequired[str]
    r"""The type of resource associated with the access token, e.g. `Company` for a company-level token or `Oauth::Application` for a system-level token.

    """
    uuid: NotRequired[str]
    r"""The UUID of the associated resource"""


class Resource(BaseModel):
    r"""The resource associated with this access token. Null when
    the token has no associated resource.

    """

    type: Optional[str] = None
    r"""The type of resource associated with the access token, e.g. `Company` for a company-level token or `Oauth::Application` for a system-level token.

    """

    uuid: Optional[str] = None
    r"""The UUID of the associated resource"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["type", "uuid"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class TokenInfoType(str, Enum, metaclass=utils.OpenEnumMeta):
    r"""The type of resource owner:
    - `CompanyAdmin`: A company administrator
    - `Employee`: An employee
    - `Contractor`: A contractor

    """

    COMPANY_ADMIN = "CompanyAdmin"
    EMPLOYEE = "Employee"
    CONTRACTOR = "Contractor"


class ResourceOwnerTypedDict(TypedDict):
    r"""The resource owner (user) who authorized this access token. Null for
    system-level tokens or when the owner cannot be determined.

    """

    type: NotRequired[TokenInfoType]
    r"""The type of resource owner:
    - `CompanyAdmin`: A company administrator
    - `Employee`: An employee
    - `Contractor`: A contractor

    """
    uuid: NotRequired[str]
    r"""The UUID of the resource owner"""


class ResourceOwner(BaseModel):
    r"""The resource owner (user) who authorized this access token. Null for
    system-level tokens or when the owner cannot be determined.

    """

    type: Optional[TokenInfoType] = None
    r"""The type of resource owner:
    - `CompanyAdmin`: A company administrator
    - `Employee`: An employee
    - `Contractor`: A contractor

    """

    uuid: Optional[str] = None
    r"""The UUID of the resource owner"""

    @field_serializer("type")
    def serialize_type(self, value):
        if isinstance(value, str):
            try:
                return models.TokenInfoType(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["type", "uuid"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class TokenInfoTypedDict(TypedDict):
    scope: NotRequired[str]
    r"""Space-separated list of OAuth scopes granted to this access token.

    """
    resource: NotRequired[Nullable[ResourceTypedDict]]
    r"""The resource associated with this access token. Null when
    the token has no associated resource.

    """
    resource_owner: NotRequired[Nullable[ResourceOwnerTypedDict]]
    r"""The resource owner (user) who authorized this access token. Null for
    system-level tokens or when the owner cannot be determined.

    """


class TokenInfo(BaseModel):
    scope: Optional[str] = None
    r"""Space-separated list of OAuth scopes granted to this access token.

    """

    resource: OptionalNullable[Resource] = UNSET
    r"""The resource associated with this access token. Null when
    the token has no associated resource.

    """

    resource_owner: OptionalNullable[ResourceOwner] = UNSET
    r"""The resource owner (user) who authorized this access token. Null for
    system-level tokens or when the owner cannot be determined.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["scope", "resource", "resource_owner"])
        nullable_fields = set(["resource", "resource_owner"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
