"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from gusto_embedded_v_2026_06_15 import models, utils
from gusto_embedded_v_2026_06_15.types import BaseModel, UNSET_SENTINEL
from pydantic import field_serializer, model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class ExternalPayrollBasicStatus(str, Enum, metaclass=utils.OpenEnumMeta):
    r"""The status of the external payroll. The status will be `unprocessed` when the external payroll is created and transition to `processed` once tax liabilities are entered and finalized.  Once in the `processed` status all actions that can edit an external payroll will be disabled."""

    UNPROCESSED = "unprocessed"
    PROCESSED = "processed"


class ExternalPayrollBasicTypedDict(TypedDict):
    r"""The representation of an external payroll with minimal information."""

    uuid: str
    r"""The UUID of the external payroll."""
    company_uuid: NotRequired[str]
    r"""The UUID of the company."""
    check_date: NotRequired[str]
    r"""External payroll's check date."""
    payment_period_start_date: NotRequired[str]
    r"""External payroll's pay period start date."""
    payment_period_end_date: NotRequired[str]
    r"""External payroll's pay period end date."""
    status: NotRequired[ExternalPayrollBasicStatus]
    r"""The status of the external payroll. The status will be `unprocessed` when the external payroll is created and transition to `processed` once tax liabilities are entered and finalized.  Once in the `processed` status all actions that can edit an external payroll will be disabled."""


class ExternalPayrollBasic(BaseModel):
    r"""The representation of an external payroll with minimal information."""

    uuid: str
    r"""The UUID of the external payroll."""

    company_uuid: Optional[str] = None
    r"""The UUID of the company."""

    check_date: Optional[str] = None
    r"""External payroll's check date."""

    payment_period_start_date: Optional[str] = None
    r"""External payroll's pay period start date."""

    payment_period_end_date: Optional[str] = None
    r"""External payroll's pay period end date."""

    status: Optional[ExternalPayrollBasicStatus] = None
    r"""The status of the external payroll. The status will be `unprocessed` when the external payroll is created and transition to `processed` once tax liabilities are entered and finalized.  Once in the `processed` status all actions that can edit an external payroll will be disabled."""

    @field_serializer("status")
    def serialize_status(self, value):
        if isinstance(value, str):
            try:
                return models.ExternalPayrollBasicStatus(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "company_uuid",
                "check_date",
                "payment_period_start_date",
                "payment_period_end_date",
                "status",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
