"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .bulk_report_company import BulkReportCompany, BulkReportCompanyTypedDict
from datetime import datetime
from enum import Enum
from gusto_embedded_v_2026_06_15 import models, utils
from gusto_embedded_v_2026_06_15.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import field_serializer, model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class BulkReportStatus(str, Enum, metaclass=utils.OpenEnumMeta):
    r"""Overall batch status. `pending`/`processing` while in progress; once finished, `success` (all reports succeeded), `partial_success` (some succeeded, some failed), or `failed` (none succeeded)."""

    PENDING = "pending"
    PROCESSING = "processing"
    SUCCESS = "success"
    PARTIAL_SUCCESS = "partial_success"
    FAILED = "failed"


class BulkReportTypedDict(TypedDict):
    uuid: str
    r"""Unique identifier of the bulk report batch."""
    status: BulkReportStatus
    r"""Overall batch status. `pending`/`processing` while in progress; once finished, `success` (all reports succeeded), `partial_success` (some succeeded, some failed), or `failed` (none succeeded)."""
    submitted_at: datetime
    r"""When the batch was accepted."""
    completed_at: Nullable[datetime]
    r"""When the batch reached a terminal state. Null while non-terminal."""
    submitted_items: int
    r"""How many reports the partner asked for in this batch."""
    partner_uuid: NotRequired[str]
    r"""UUID of the partner that owns this batch. Returned only once the batch has finished; omitted while in progress."""
    processed_items: NotRequired[int]
    r"""How many reports succeeded. Returned only once the batch has finished; omitted while in progress."""
    report_url: NotRequired[Nullable[str]]
    r"""Signed S3 URL to a zip containing every successfully-generated report, valid for 10 minutes. Returned only once the batch has finished; omitted while in progress."""
    companies: NotRequired[List[BulkReportCompanyTypedDict]]
    r"""Per-company breakdown. Returned only once the batch has finished; omitted while in progress."""


class BulkReport(BaseModel):
    uuid: str
    r"""Unique identifier of the bulk report batch."""

    status: BulkReportStatus
    r"""Overall batch status. `pending`/`processing` while in progress; once finished, `success` (all reports succeeded), `partial_success` (some succeeded, some failed), or `failed` (none succeeded)."""

    submitted_at: datetime
    r"""When the batch was accepted."""

    completed_at: Nullable[datetime]
    r"""When the batch reached a terminal state. Null while non-terminal."""

    submitted_items: int
    r"""How many reports the partner asked for in this batch."""

    partner_uuid: Optional[str] = None
    r"""UUID of the partner that owns this batch. Returned only once the batch has finished; omitted while in progress."""

    processed_items: Optional[int] = None
    r"""How many reports succeeded. Returned only once the batch has finished; omitted while in progress."""

    report_url: OptionalNullable[str] = UNSET
    r"""Signed S3 URL to a zip containing every successfully-generated report, valid for 10 minutes. Returned only once the batch has finished; omitted while in progress."""

    companies: Optional[List[BulkReportCompany]] = None
    r"""Per-company breakdown. Returned only once the batch has finished; omitted while in progress."""

    @field_serializer("status")
    def serialize_status(self, value):
        if isinstance(value, str):
            try:
                return models.BulkReportStatus(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["partner_uuid", "processed_items", "report_url", "companies"]
        )
        nullable_fields = set(["completed_at", "report_url"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
