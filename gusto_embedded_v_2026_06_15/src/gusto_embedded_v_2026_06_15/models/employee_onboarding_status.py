"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from gusto_embedded_v_2026_06_15.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class EmployeeOnboardingStatusOnboardingStepTypedDict(TypedDict):
    title: NotRequired[str]
    r"""User-friendly description of the onboarding step."""
    id: NotRequired[str]
    r"""String identifier for the onboarding step."""
    required: NotRequired[bool]
    r"""When true, this step is required."""
    completed: NotRequired[bool]
    r"""When true, this step has been completed."""
    requirements: NotRequired[List[str]]
    r"""A list of onboarding steps required to begin this step."""


class EmployeeOnboardingStatusOnboardingStep(BaseModel):
    title: Optional[str] = None
    r"""User-friendly description of the onboarding step."""

    id: Optional[str] = None
    r"""String identifier for the onboarding step."""

    required: Optional[bool] = None
    r"""When true, this step is required."""

    completed: Optional[bool] = None
    r"""When true, this step has been completed."""

    requirements: Optional[List[str]] = None
    r"""A list of onboarding steps required to begin this step."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["title", "id", "required", "completed", "requirements"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class FieldT(str, Enum):
    r"""The employee field affected."""

    SSN = "ssn"


class EmployeeOnboardingStatusCategory(str, Enum):
    r"""Category of the blocker. See the array-level description for resolution guidance."""

    DUPLICATE_VALUE = "duplicate_value"


class BlockersTypedDict(TypedDict):
    field: NotRequired[FieldT]
    r"""The employee field affected."""
    category: NotRequired[EmployeeOnboardingStatusCategory]
    r"""Category of the blocker. See the array-level description for resolution guidance."""
    message: NotRequired[str]
    r"""Human-readable description of the blocker."""


class Blockers(BaseModel):
    field: Optional[FieldT] = None
    r"""The employee field affected."""

    category: Optional[EmployeeOnboardingStatusCategory] = None
    r"""Category of the blocker. See the array-level description for resolution guidance."""

    message: Optional[str] = None
    r"""Human-readable description of the blocker."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["field", "category", "message"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class EmployeeOnboardingStatusTypedDict(TypedDict):
    r"""The representation of an employee's onboarding status."""

    uuid: str
    r"""Unique identifier for this employee."""
    onboarding_status: NotRequired[str]
    r"""One of the \"onboarding_status\" enum values."""
    onboarding_steps: NotRequired[List[EmployeeOnboardingStatusOnboardingStepTypedDict]]
    r"""List of steps required to onboard an employee."""
    blockers: NotRequired[List[BlockersTypedDict]]
    r"""Validation issues that should be resolved before this employee's onboarding is complete. Each entry identifies an affected field, a category describing the type of problem, and a human-readable message.

    Supported categories:

    - `duplicate_value`: Another employee in the same company already has this value. To resolve, cancel this onboarding and initiate a rehire if it's a returning employee, or contact support to investigate the conflict.

    This list may grow over time as new validation rules are added.

    """


class EmployeeOnboardingStatus(BaseModel):
    r"""The representation of an employee's onboarding status."""

    uuid: str
    r"""Unique identifier for this employee."""

    onboarding_status: Optional[str] = None
    r"""One of the \"onboarding_status\" enum values."""

    onboarding_steps: Optional[List[EmployeeOnboardingStatusOnboardingStep]] = None
    r"""List of steps required to onboard an employee."""

    blockers: Optional[List[Blockers]] = None
    r"""Validation issues that should be resolved before this employee's onboarding is complete. Each entry identifies an affected field, a category describing the type of problem, and a human-readable message.

    Supported categories:

    - `duplicate_value`: Another employee in the same company already has this value. To resolve, cancel this onboarding and initiate a rehire if it's a returning employee, or contact support to investigate the conflict.

    This list may grow over time as new validation rules are added.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["onboarding_status", "onboarding_steps", "blockers"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
