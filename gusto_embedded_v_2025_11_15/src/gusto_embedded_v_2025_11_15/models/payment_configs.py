"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from gusto_embedded_v_2025_11_15 import models, utils
from gusto_embedded_v_2025_11_15.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import field_serializer, model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class PaymentSpeed(str, Enum, metaclass=utils.OpenEnumMeta):
    r"""Payment speed. READ-ONLY.
    - `1-day`: Next-day ACH (only for partners that opt in).
    - `2-day`: Two-day ACH.
    - `4-day`: Standard ACH.

    """

    ONE_MINUS_DAY = "1-day"
    TWO_MINUS_DAY = "2-day"
    FOUR_MINUS_DAY = "4-day"


class BlockerType(str, Enum, metaclass=utils.OpenEnumMeta):
    r"""The type of blocker"""

    MINIMUM_DAYS = "minimum_days"
    MINIMUM_FUNDED_PAYMENTS = "minimum_funded_payments"


class EarnedFastAchBlockersTypedDict(TypedDict):
    blocker_type: NotRequired[BlockerType]
    r"""The type of blocker"""
    threshold: NotRequired[float]
    r"""The threshold needed to unblock"""


class EarnedFastAchBlockers(BaseModel):
    blocker_type: Optional[BlockerType] = None
    r"""The type of blocker"""

    threshold: Optional[float] = None
    r"""The threshold needed to unblock"""

    @field_serializer("blocker_type")
    def serialize_blocker_type(self, value):
        if isinstance(value, str):
            try:
                return models.BlockerType(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["blocker_type", "threshold"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PaymentConfigsTypedDict(TypedDict):
    company_uuid: NotRequired[str]
    r"""Company uuid"""
    partner_uuid: NotRequired[str]
    r"""Partner uuid"""
    fast_payment_limit: NotRequired[Nullable[str]]
    r"""Payment limit for 1-day or 2-day payroll (string representation of decimal)."""
    payment_speed: NotRequired[PaymentSpeed]
    r"""Payment speed. READ-ONLY.
    - `1-day`: Next-day ACH (only for partners that opt in).
    - `2-day`: Two-day ACH.
    - `4-day`: Standard ACH.

    """
    partner_owned_disbursement: NotRequired[bool]
    r"""Whether the company is configured to use the partner-owned disbursement payment rail"""
    earned_fast_ach_blockers: NotRequired[List[EarnedFastAchBlockersTypedDict]]
    r"""Blockers preventing the company from earning fast ACH payments"""


class PaymentConfigs(BaseModel):
    company_uuid: Optional[str] = None
    r"""Company uuid"""

    partner_uuid: Optional[str] = None
    r"""Partner uuid"""

    fast_payment_limit: OptionalNullable[str] = UNSET
    r"""Payment limit for 1-day or 2-day payroll (string representation of decimal)."""

    payment_speed: Optional[PaymentSpeed] = None
    r"""Payment speed. READ-ONLY.
    - `1-day`: Next-day ACH (only for partners that opt in).
    - `2-day`: Two-day ACH.
    - `4-day`: Standard ACH.

    """

    partner_owned_disbursement: Optional[bool] = None
    r"""Whether the company is configured to use the partner-owned disbursement payment rail"""

    earned_fast_ach_blockers: Optional[List[EarnedFastAchBlockers]] = None
    r"""Blockers preventing the company from earning fast ACH payments"""

    @field_serializer("payment_speed")
    def serialize_payment_speed(self, value):
        if isinstance(value, str):
            try:
                return models.PaymentSpeed(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "company_uuid",
                "partner_uuid",
                "fast_payment_limit",
                "payment_speed",
                "partner_owned_disbursement",
                "earned_fast_ach_blockers",
            ]
        )
        nullable_fields = set(["fast_payment_limit"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
