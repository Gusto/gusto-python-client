"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from gusto_embedded_v_2025_11_15.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class TimeOffPolicyRequestPolicyType(str, Enum):
    r"""Type of the time off policy. Currently only \"vacation\" and \"sick\" are supported"""

    VACATION = "vacation"
    SICK = "sick"


class AccrualMethod(str, Enum):
    r"""Accrual method of the time off policy"""

    UNLIMITED = "unlimited"
    PER_PAY_PERIOD = "per_pay_period"
    PER_CALENDAR_YEAR = "per_calendar_year"
    PER_ANNIVERSARY_YEAR = "per_anniversary_year"
    PER_HOUR_WORKED = "per_hour_worked"
    PER_HOUR_WORKED_NO_OVERTIME = "per_hour_worked_no_overtime"
    PER_HOUR_PAID = "per_hour_paid"
    PER_HOUR_PAID_NO_OVERTIME = "per_hour_paid_no_overtime"


class TimeOffPolicyRequestTypedDict(TypedDict):
    r"""Request body for creating a time off policy"""

    name: str
    r"""Name of the time off policy"""
    policy_type: TimeOffPolicyRequestPolicyType
    r"""Type of the time off policy. Currently only \"vacation\" and \"sick\" are supported"""
    accrual_method: AccrualMethod
    r"""Accrual method of the time off policy"""
    accrual_rate: NotRequired[Nullable[str]]
    r"""The rate at which the time off hours will accrue for an employee on the policy. Represented as a float, e.g. \"40.0\"."""
    accrual_rate_unit: NotRequired[Nullable[str]]
    r"""The number of hours an employee has to work or be paid for to accrue the number of hours set in the accrual rate. Only used for hourly policies (per_hour_paid, per_hour_paid_no_overtime, per_hour_work, per_hour_worked_no_overtime). Represented as a float, e.g. \"40.0\"."""
    paid_out_on_termination: NotRequired[bool]
    r"""Boolean representing if an employee's accrued time off hours will be paid out on termination. If accrual_method is unlimited, then paid_out_on_termination must be `false`."""
    accrual_waiting_period_days: NotRequired[Nullable[int]]
    r"""Number of days before an employee on the policy will begin accruing time off hours. If accrual_method is per_anniversary_year, per_calendar_year, or unlimited, then accrual_waiting_period_days should be 0."""
    carryover_limit_hours: NotRequired[Nullable[str]]
    r"""The max number of hours an employee can carryover from one year to the next. If accrual_method is unlimited, then carryover_limit_hours must be blank."""
    max_accrual_hours_per_year: NotRequired[Nullable[str]]
    r"""The max number of hours an employee can accrue in a year. If accrual_method is yearly (per_anniversary_year, per_calendar_year) or unlimited, then max_accrual_hours_per_year must be blank."""
    max_hours: NotRequired[Nullable[str]]
    r"""The max number of hours an employee can accrue. If accrual_method is unlimited, then max_hours must be blank."""
    policy_reset_date: NotRequired[Nullable[str]]
    r"""The date the policy resets. Format MM-DD"""
    complete: NotRequired[bool]
    r"""boolean representing if a policy has completed configuration"""


class TimeOffPolicyRequest(BaseModel):
    r"""Request body for creating a time off policy"""

    name: str
    r"""Name of the time off policy"""

    policy_type: TimeOffPolicyRequestPolicyType
    r"""Type of the time off policy. Currently only \"vacation\" and \"sick\" are supported"""

    accrual_method: AccrualMethod
    r"""Accrual method of the time off policy"""

    accrual_rate: OptionalNullable[str] = UNSET
    r"""The rate at which the time off hours will accrue for an employee on the policy. Represented as a float, e.g. \"40.0\"."""

    accrual_rate_unit: OptionalNullable[str] = UNSET
    r"""The number of hours an employee has to work or be paid for to accrue the number of hours set in the accrual rate. Only used for hourly policies (per_hour_paid, per_hour_paid_no_overtime, per_hour_work, per_hour_worked_no_overtime). Represented as a float, e.g. \"40.0\"."""

    paid_out_on_termination: Optional[bool] = None
    r"""Boolean representing if an employee's accrued time off hours will be paid out on termination. If accrual_method is unlimited, then paid_out_on_termination must be `false`."""

    accrual_waiting_period_days: OptionalNullable[int] = UNSET
    r"""Number of days before an employee on the policy will begin accruing time off hours. If accrual_method is per_anniversary_year, per_calendar_year, or unlimited, then accrual_waiting_period_days should be 0."""

    carryover_limit_hours: OptionalNullable[str] = UNSET
    r"""The max number of hours an employee can carryover from one year to the next. If accrual_method is unlimited, then carryover_limit_hours must be blank."""

    max_accrual_hours_per_year: OptionalNullable[str] = UNSET
    r"""The max number of hours an employee can accrue in a year. If accrual_method is yearly (per_anniversary_year, per_calendar_year) or unlimited, then max_accrual_hours_per_year must be blank."""

    max_hours: OptionalNullable[str] = UNSET
    r"""The max number of hours an employee can accrue. If accrual_method is unlimited, then max_hours must be blank."""

    policy_reset_date: OptionalNullable[str] = UNSET
    r"""The date the policy resets. Format MM-DD"""

    complete: Optional[bool] = None
    r"""boolean representing if a policy has completed configuration"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "accrual_rate",
                "accrual_rate_unit",
                "paid_out_on_termination",
                "accrual_waiting_period_days",
                "carryover_limit_hours",
                "max_accrual_hours_per_year",
                "max_hours",
                "policy_reset_date",
                "complete",
            ]
        )
        nullable_fields = set(
            [
                "accrual_rate",
                "accrual_rate_unit",
                "accrual_waiting_period_days",
                "carryover_limit_hours",
                "max_accrual_hours_per_year",
                "max_hours",
                "policy_reset_date",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
