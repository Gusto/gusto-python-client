"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .entity_error_object import EntityErrorObject, EntityErrorObjectTypedDict
from datetime import date, datetime
from enum import Enum
from gusto_app_integration_v_2026_06_15 import models, utils
from gusto_app_integration_v_2026_06_15.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import field_serializer, model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class PayrollSyncStatus(str, Enum, metaclass=utils.OpenEnumMeta):
    r"""The current status of the payroll sync. READ-ONLY.

    ## State Transitions
    ```
    pending ──────► in_progress ──────► success
    ├─────► failure
    └─────► partial_success
    ```

    ### Valid Transitions
    - `pending` → `in_progress`: Automatic when processing begins
    - `in_progress` → `success`: All time sheet data synced successfully
    - `in_progress` → `failure`: Sync failed entirely
    - `in_progress` → `partial_success`: Some members failed to sync

    """

    PENDING = "pending"
    IN_PROGRESS = "in_progress"
    SUCCESS = "success"
    FAILURE = "failure"
    PARTIAL_SUCCESS = "partial_success"


class PayrollSyncKind(str, Enum):
    r"""The kind of payroll sync.
    - `regular`: A regular payroll sync

    """

    REGULAR = "regular"


class PayrollSyncTypedDict(TypedDict):
    r"""Record representing the sync of time sheet data to payroll."""

    uuid: NotRequired[str]
    r"""Unique identifier of the payroll sync."""
    company_uuid: NotRequired[str]
    r"""Unique identifier of the company to which the payroll sync belongs."""
    status: NotRequired[PayrollSyncStatus]
    r"""The current status of the payroll sync. READ-ONLY.

    ## State Transitions
    ```
    pending ──────► in_progress ──────► success
    ├─────► failure
    └─────► partial_success
    ```

    ### Valid Transitions
    - `pending` → `in_progress`: Automatic when processing begins
    - `in_progress` → `success`: All time sheet data synced successfully
    - `in_progress` → `failure`: Sync failed entirely
    - `in_progress` → `partial_success`: Some members failed to sync

    """
    kind: NotRequired[PayrollSyncKind]
    r"""The kind of payroll sync.
    - `regular`: A regular payroll sync

    """
    pay_schedule_uuid: NotRequired[str]
    r"""Unique identifier of the pay schedule associated with this sync."""
    pay_period_start_date: NotRequired[date]
    r"""The start date of the pay period per ISO 8601 format."""
    pay_period_end_date: NotRequired[date]
    r"""The end date of the pay period per ISO 8601 format."""
    submitted_at: NotRequired[datetime]
    r"""Datetime for when the payroll sync was submitted."""
    completed_at: NotRequired[Nullable[datetime]]
    r"""Datetime for when the payroll sync completed. Null if the sync is still in progress."""
    errors: NotRequired[List[EntityErrorObjectTypedDict]]
    r"""Errors encountered during the sync, if any. Each error follows the standard entity error format."""
    warnings: NotRequired[List[str]]
    r"""Warnings encountered during the sync, if any."""


class PayrollSync(BaseModel):
    r"""Record representing the sync of time sheet data to payroll."""

    uuid: Optional[str] = None
    r"""Unique identifier of the payroll sync."""

    company_uuid: Optional[str] = None
    r"""Unique identifier of the company to which the payroll sync belongs."""

    status: Optional[PayrollSyncStatus] = None
    r"""The current status of the payroll sync. READ-ONLY.

    ## State Transitions
    ```
    pending ──────► in_progress ──────► success
    ├─────► failure
    └─────► partial_success
    ```

    ### Valid Transitions
    - `pending` → `in_progress`: Automatic when processing begins
    - `in_progress` → `success`: All time sheet data synced successfully
    - `in_progress` → `failure`: Sync failed entirely
    - `in_progress` → `partial_success`: Some members failed to sync

    """

    kind: Optional[PayrollSyncKind] = None
    r"""The kind of payroll sync.
    - `regular`: A regular payroll sync

    """

    pay_schedule_uuid: Optional[str] = None
    r"""Unique identifier of the pay schedule associated with this sync."""

    pay_period_start_date: Optional[date] = None
    r"""The start date of the pay period per ISO 8601 format."""

    pay_period_end_date: Optional[date] = None
    r"""The end date of the pay period per ISO 8601 format."""

    submitted_at: Optional[datetime] = None
    r"""Datetime for when the payroll sync was submitted."""

    completed_at: OptionalNullable[datetime] = UNSET
    r"""Datetime for when the payroll sync completed. Null if the sync is still in progress."""

    errors: Optional[List[EntityErrorObject]] = None
    r"""Errors encountered during the sync, if any. Each error follows the standard entity error format."""

    warnings: Optional[List[str]] = None
    r"""Warnings encountered during the sync, if any."""

    @field_serializer("status")
    def serialize_status(self, value):
        if isinstance(value, str):
            try:
                return models.PayrollSyncStatus(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "uuid",
                "company_uuid",
                "status",
                "kind",
                "pay_schedule_uuid",
                "pay_period_start_date",
                "pay_period_end_date",
                "submitted_at",
                "completed_at",
                "errors",
                "warnings",
            ]
        )
        nullable_fields = set(["completed_at"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
