"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from gusto_embedded.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing_extensions import NotRequired, TypedDict


class BulkReportGeneralLedgerItemReportType(str, Enum):
    r"""Identifies this batch item as a general ledger report."""

    GENERAL_LEDGER = "general_ledger"


class Aggregation(str, Enum):
    r"""The breakdown of the report. Use `default` for no split."""

    DEFAULT = "default"
    JOB = "job"
    DEPARTMENT = "department"
    INTEGRATION = "integration"


class BulkReportGeneralLedgerItemTypedDict(TypedDict):
    r"""A general ledger report entry in a bulk batch."""

    company_uuid: str
    r"""UUID of the company to generate the report for. The partner must be mapped to this company."""
    report_type: BulkReportGeneralLedgerItemReportType
    r"""Identifies this batch item as a general ledger report."""
    payroll_uuid: str
    r"""The UUID of the payroll to generate the general ledger for."""
    aggregation: Aggregation
    r"""The breakdown of the report. Use `default` for no split."""
    integration_type: NotRequired[Nullable[str]]
    r"""The kind of integration set up for the company. Required when `aggregation` is `integration`; must be null otherwise."""


class BulkReportGeneralLedgerItem(BaseModel):
    r"""A general ledger report entry in a bulk batch."""

    company_uuid: str
    r"""UUID of the company to generate the report for. The partner must be mapped to this company."""

    report_type: BulkReportGeneralLedgerItemReportType
    r"""Identifies this batch item as a general ledger report."""

    payroll_uuid: str
    r"""The UUID of the payroll to generate the general ledger for."""

    aggregation: Aggregation
    r"""The breakdown of the report. Use `default` for no split."""

    integration_type: OptionalNullable[str] = UNSET
    r"""The kind of integration set up for the company. Required when `aggregation` is `integration`; must be null otherwise."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["integration_type"])
        nullable_fields = set(["integration_type"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
