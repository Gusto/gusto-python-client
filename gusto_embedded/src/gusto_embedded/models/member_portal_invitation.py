"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from gusto_embedded.types import BaseModel
from typing_extensions import TypedDict


class MemberPortalInvitationStatus1(str, Enum):
    r"""Current status of the member portal invitation.
    - `pending`: The invitation record has been created but the invitation email has not yet been delivered.
    - `sent`: The invitation email has been delivered to the member.
    - `verified`: The member has been verified by the member portal partner.
    - `complete`: The member has fully completed their member portal registration.
    - `cancelled`: The invitation has been cancelled, either via `DELETE` on this endpoint or because it was superseded.

    """

    PENDING = "pending"
    SENT = "sent"
    VERIFIED = "verified"
    COMPLETE = "complete"
    CANCELLED = "cancelled"


class MemberPortalInvitationTypedDict(TypedDict):
    r"""The status of a member portal invitation for an employee or contractor. Returned by `GET /v1/employees/{employee_id}/member_portal_invitations` and `GET /v1/contractors/{contractor_uuid}/member_portal_invitations`."""

    status: MemberPortalInvitationStatus1
    r"""Current status of the member portal invitation.
    - `pending`: The invitation record has been created but the invitation email has not yet been delivered.
    - `sent`: The invitation email has been delivered to the member.
    - `verified`: The member has been verified by the member portal partner.
    - `complete`: The member has fully completed their member portal registration.
    - `cancelled`: The invitation has been cancelled, either via `DELETE` on this endpoint or because it was superseded.

    """
    expired: bool
    r"""Whether the invitation can no longer be acted on by the member. Returns `true` when the invitation token has expired or when the invitation has been `cancelled`. To reissue an expired invitation, call `POST` on this endpoint again — a new token will be generated and will override the prior one.

    """


class MemberPortalInvitation(BaseModel):
    r"""The status of a member portal invitation for an employee or contractor. Returned by `GET /v1/employees/{employee_id}/member_portal_invitations` and `GET /v1/contractors/{contractor_uuid}/member_portal_invitations`."""

    status: MemberPortalInvitationStatus1
    r"""Current status of the member portal invitation.
    - `pending`: The invitation record has been created but the invitation email has not yet been delivered.
    - `sent`: The invitation email has been delivered to the member.
    - `verified`: The member has been verified by the member portal partner.
    - `complete`: The member has fully completed their member portal registration.
    - `cancelled`: The invitation has been cancelled, either via `DELETE` on this endpoint or because it was superseded.

    """

    expired: bool
    r"""Whether the invitation can no longer be acted on by the member. Returns `true` when the invitation token has expired or when the invitation has been `cancelled`. To reissue an expired invitation, call `POST` on this endpoint again — a new token will be generated and will override the prior one.

    """
