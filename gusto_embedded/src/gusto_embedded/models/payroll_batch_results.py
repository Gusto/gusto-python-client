"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from enum import Enum
from gusto_embedded.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class PayrollBatchResultsStatus(str, Enum):
    r"""The lifecycle status of the batch request itself. Terminal values are `completed` (processing finished — inspect `results` and `exclusions` for per-payroll outcomes) and `failed` (the batch crashed at the system level; can be retried). This is distinct from the per-payroll `status` returned inside `results[]`. A `completed` batch does not imply every payroll was cancelled."""

    PENDING = "pending"
    PROCESSING = "processing"
    COMPLETED = "completed"
    FAILED = "failed"


class PayrollBatchResultsResultsStatus(str, Enum):
    r"""The outcome of cancelling this payroll. A cancel is atomic — there is no per-payroll `partial_success`.
    - `success`: the payroll was cancelled, or required no action (already cancelled / never run)
    - `failed`: the payroll could not be cancelled; see `errors`

    """

    SUCCESS = "success"
    FAILED = "failed"


class PayrollBatchResultsResultsCategory(str, Enum):
    r"""Machine-readable reason the cancellation failed.
    - `not_cancellable`: the payroll is past the point where it can be cancelled
    - `internal_error`: an unexpected error occurred; the request can be retried

    """

    NOT_CANCELLABLE = "not_cancellable"
    INTERNAL_ERROR = "internal_error"


class PayrollBatchResultsErrorsTypedDict(TypedDict):
    error_key: NotRequired[str]
    r"""The key identifying the error source."""
    category: NotRequired[PayrollBatchResultsResultsCategory]
    r"""Machine-readable reason the cancellation failed.
    - `not_cancellable`: the payroll is past the point where it can be cancelled
    - `internal_error`: an unexpected error occurred; the request can be retried

    """
    message: NotRequired[str]
    r"""Human-readable explanation of the failure."""


class PayrollBatchResultsErrors(BaseModel):
    error_key: Optional[str] = None
    r"""The key identifying the error source."""

    category: Optional[PayrollBatchResultsResultsCategory] = None
    r"""Machine-readable reason the cancellation failed.
    - `not_cancellable`: the payroll is past the point where it can be cancelled
    - `internal_error`: an unexpected error occurred; the request can be retried

    """

    message: Optional[str] = None
    r"""Human-readable explanation of the failure."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["error_key", "category", "message"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class ResultsTypedDict(TypedDict):
    idx: NotRequired[int]
    r"""The index of this payroll in the original POST batch array."""
    uuid: NotRequired[str]
    r"""The UUID of the payroll."""
    status: NotRequired[PayrollBatchResultsResultsStatus]
    r"""The outcome of cancelling this payroll. A cancel is atomic — there is no per-payroll `partial_success`.
    - `success`: the payroll was cancelled, or required no action (already cancelled / never run)
    - `failed`: the payroll could not be cancelled; see `errors`

    """
    errors: NotRequired[List[PayrollBatchResultsErrorsTypedDict]]
    r"""Present only when `status` is `failed`. A cancel is a single atomic operation, so this is a flat array with exactly one error."""


class Results(BaseModel):
    idx: Optional[int] = None
    r"""The index of this payroll in the original POST batch array."""

    uuid: Optional[str] = None
    r"""The UUID of the payroll."""

    status: Optional[PayrollBatchResultsResultsStatus] = None
    r"""The outcome of cancelling this payroll. A cancel is atomic — there is no per-payroll `partial_success`.
    - `success`: the payroll was cancelled, or required no action (already cancelled / never run)
    - `failed`: the payroll could not be cancelled; see `errors`

    """

    errors: Optional[List[PayrollBatchResultsErrors]] = None
    r"""Present only when `status` is `failed`. A cancel is a single atomic operation, so this is a flat array with exactly one error."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["idx", "uuid", "status", "errors"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PayrollBatchResultsEntityType(str, Enum):
    r"""The type of entity this exclusion represents."""

    PAYROLL = "payroll"


class PayrollBatchResultsExclusionsStatus(str, Enum):
    r"""Always `failed` for an excluded payroll."""

    FAILED = "failed"


class PayrollBatchResultsCategory(str, Enum):
    r"""Machine-readable category for why the payroll was excluded.
    - `not_found`: the payroll does not exist, or is not associated with a company the partner is mapped to
    - `duplicate_operation`: the same payroll UUID appeared more than once in the request; only the first occurrence is processed

    """

    NOT_FOUND = "not_found"
    DUPLICATE_OPERATION = "duplicate_operation"


class ExclusionsTypedDict(TypedDict):
    idx: NotRequired[int]
    r"""The index of this payroll in the original POST batch array."""
    entity_type: NotRequired[PayrollBatchResultsEntityType]
    r"""The type of entity this exclusion represents."""
    uuid: NotRequired[str]
    r"""The UUID of the excluded payroll."""
    company_uuid: NotRequired[str]
    r"""The UUID of the company asserted to own the payroll."""
    status: NotRequired[PayrollBatchResultsExclusionsStatus]
    r"""Always `failed` for an excluded payroll."""
    category: NotRequired[PayrollBatchResultsCategory]
    r"""Machine-readable category for why the payroll was excluded.
    - `not_found`: the payroll does not exist, or is not associated with a company the partner is mapped to
    - `duplicate_operation`: the same payroll UUID appeared more than once in the request; only the first occurrence is processed

    """
    message: NotRequired[str]
    r"""Human-readable explanation for the exclusion."""


class Exclusions(BaseModel):
    idx: Optional[int] = None
    r"""The index of this payroll in the original POST batch array."""

    entity_type: Optional[PayrollBatchResultsEntityType] = None
    r"""The type of entity this exclusion represents."""

    uuid: Optional[str] = None
    r"""The UUID of the excluded payroll."""

    company_uuid: Optional[str] = None
    r"""The UUID of the company asserted to own the payroll."""

    status: Optional[PayrollBatchResultsExclusionsStatus] = None
    r"""Always `failed` for an excluded payroll."""

    category: Optional[PayrollBatchResultsCategory] = None
    r"""Machine-readable category for why the payroll was excluded.
    - `not_found`: the payroll does not exist, or is not associated with a company the partner is mapped to
    - `duplicate_operation`: the same payroll UUID appeared more than once in the request; only the first occurrence is processed

    """

    message: Optional[str] = None
    r"""Human-readable explanation for the exclusion."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "idx",
                "entity_type",
                "uuid",
                "company_uuid",
                "status",
                "category",
                "message",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PayrollBatchResultsTypedDict(TypedDict):
    r"""A payroll cancellation batch with per-payroll results."""

    uuid: str
    r"""The unique identifier of the payroll cancellation batch."""
    idempotency_key: str
    r"""The idempotency key provided when creating the batch."""
    status: PayrollBatchResultsStatus
    r"""The lifecycle status of the batch request itself. Terminal values are `completed` (processing finished — inspect `results` and `exclusions` for per-payroll outcomes) and `failed` (the batch crashed at the system level; can be retried). This is distinct from the per-payroll `status` returned inside `results[]`. A `completed` batch does not imply every payroll was cancelled."""
    submitted_at: datetime
    r"""The timestamp when the batch was submitted."""
    completed_at: NotRequired[Nullable[datetime]]
    r"""The timestamp when the batch processing completed."""
    submitted_items: NotRequired[Nullable[int]]
    r"""The number of payrolls submitted in the batch."""
    processed_items: NotRequired[int]
    r"""The number of payrolls processed (cancelled or attempted). Only present once the batch reaches a terminal status."""
    excluded_items: NotRequired[int]
    r"""The number of payrolls excluded from processing. Only present once the batch reaches a terminal status."""
    results: NotRequired[List[ResultsTypedDict]]
    r"""Per-payroll cancellation results. Only present once the batch reaches a terminal status. One entry per authorized payroll."""
    exclusions: NotRequired[List[ExclusionsTypedDict]]
    r"""Payrolls that could not be processed, determined at submission time. Only present once the batch reaches a terminal status. Every UUID submitted in the POST batch appears in exactly one of `results` or `exclusions`."""


class PayrollBatchResults(BaseModel):
    r"""A payroll cancellation batch with per-payroll results."""

    uuid: str
    r"""The unique identifier of the payroll cancellation batch."""

    idempotency_key: str
    r"""The idempotency key provided when creating the batch."""

    status: PayrollBatchResultsStatus
    r"""The lifecycle status of the batch request itself. Terminal values are `completed` (processing finished — inspect `results` and `exclusions` for per-payroll outcomes) and `failed` (the batch crashed at the system level; can be retried). This is distinct from the per-payroll `status` returned inside `results[]`. A `completed` batch does not imply every payroll was cancelled."""

    submitted_at: datetime
    r"""The timestamp when the batch was submitted."""

    completed_at: OptionalNullable[datetime] = UNSET
    r"""The timestamp when the batch processing completed."""

    submitted_items: OptionalNullable[int] = UNSET
    r"""The number of payrolls submitted in the batch."""

    processed_items: Optional[int] = None
    r"""The number of payrolls processed (cancelled or attempted). Only present once the batch reaches a terminal status."""

    excluded_items: Optional[int] = None
    r"""The number of payrolls excluded from processing. Only present once the batch reaches a terminal status."""

    results: Optional[List[Results]] = None
    r"""Per-payroll cancellation results. Only present once the batch reaches a terminal status. One entry per authorized payroll."""

    exclusions: Optional[List[Exclusions]] = None
    r"""Payrolls that could not be processed, determined at submission time. Only present once the batch reaches a terminal status. Every UUID submitted in the POST batch appears in exactly one of `results` or `exclusions`."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "completed_at",
                "submitted_items",
                "processed_items",
                "excluded_items",
                "results",
                "exclusions",
            ]
        )
        nullable_fields = set(["completed_at", "submitted_items"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
