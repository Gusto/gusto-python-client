"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from gusto_embedded.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class ContractorPaymentDetailsListPaymentMethod(str, Enum):
    DIRECT_DEPOSIT = "Direct Deposit"
    CHECK = "Check"


class ContractorPaymentDetailsListSplitBy(str, Enum):
    AMOUNT = "Amount"
    PERCENTAGE = "Percentage"


class SplitsTypedDict(TypedDict):
    bank_account_uuid: NotRequired[str]
    name: NotRequired[str]
    hidden_account_number: NotRequired[str]
    r"""An obfuscated version of the account number which can be used for display purposes."""
    encrypted_account_number: NotRequired[Nullable[str]]
    r"""Ciphertext containing the full bank account number, which must be decrypted using a key provided by Gusto. Only visible with the `contractor_payment_methods:read:account_number` scope."""
    routing_number: NotRequired[str]
    priority: NotRequired[int]
    r"""The order of priority for each payment split, with priority 1 being the first bank account paid. Priority must be unique and sequential."""
    split_amount: NotRequired[Nullable[float]]
    r"""If `split_by` is 'Amount', this is in cents (e.g., 500 for $5.00) and exactly one account must have a `split_amount` of `null` to capture the remainder. If `split_by` is 'Percentage', this is the percentage value (e.g., 60 for 60%)."""
    account_type: NotRequired[str]


class Splits(BaseModel):
    bank_account_uuid: Optional[str] = None

    name: Optional[str] = None

    hidden_account_number: Optional[str] = None
    r"""An obfuscated version of the account number which can be used for display purposes."""

    encrypted_account_number: OptionalNullable[str] = UNSET
    r"""Ciphertext containing the full bank account number, which must be decrypted using a key provided by Gusto. Only visible with the `contractor_payment_methods:read:account_number` scope."""

    routing_number: Optional[str] = None

    priority: Optional[int] = None
    r"""The order of priority for each payment split, with priority 1 being the first bank account paid. Priority must be unique and sequential."""

    split_amount: OptionalNullable[float] = UNSET
    r"""If `split_by` is 'Amount', this is in cents (e.g., 500 for $5.00) and exactly one account must have a `split_amount` of `null` to capture the remainder. If `split_by` is 'Percentage', this is the percentage value (e.g., 60 for 60%)."""

    account_type: Optional[str] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "bank_account_uuid",
                "name",
                "hidden_account_number",
                "encrypted_account_number",
                "routing_number",
                "priority",
                "split_amount",
                "account_type",
            ]
        )
        nullable_fields = set(["encrypted_account_number", "split_amount"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class ContractorPaymentDetailsListTypedDict(TypedDict):
    contractor_uuid: NotRequired[str]
    payment_method: NotRequired[ContractorPaymentDetailsListPaymentMethod]
    first_name: NotRequired[str]
    last_name: NotRequired[str]
    split_by: NotRequired[Nullable[ContractorPaymentDetailsListSplitBy]]
    r"""Describes how the payment will be split. If split_by is Percentage, then the split amounts must add up to exactly 100. If split_by is Amount, then the amount represents cents and the last split amount must be `null` to capture the remainder."""
    splits: NotRequired[Nullable[List[SplitsTypedDict]]]


class ContractorPaymentDetailsList(BaseModel):
    contractor_uuid: Optional[str] = None

    payment_method: Optional[ContractorPaymentDetailsListPaymentMethod] = None

    first_name: Optional[str] = None

    last_name: Optional[str] = None

    split_by: OptionalNullable[ContractorPaymentDetailsListSplitBy] = UNSET
    r"""Describes how the payment will be split. If split_by is Percentage, then the split amounts must add up to exactly 100. If split_by is Amount, then the amount represents cents and the last split amount must be `null` to capture the remainder."""

    splits: OptionalNullable[List[Splits]] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "contractor_uuid",
                "payment_method",
                "first_name",
                "last_name",
                "split_by",
                "splits",
            ]
        )
        nullable_fields = set(["split_by", "splits"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
