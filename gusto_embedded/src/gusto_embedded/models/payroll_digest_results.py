"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import date, datetime
from enum import Enum
from gusto_embedded.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class PayrollDigestResultsStatus(str, Enum):
    r"""The lifecycle status of the batch request itself. Terminal values are `completed` (processing finished — inspect `results` and `exclusions` for per-company outcomes) and `failed` (request failed; can be retried). This is distinct from the per-company `status` returned inside `results[]` and `exclusions[]`."""

    PENDING = "pending"
    PROCESSING = "processing"
    COMPLETED = "completed"
    FAILED = "failed"


class PayrollDigestResultsEntityType(str, Enum):
    r"""The type of entity this result represents."""

    COMPANY = "company"


class PayrollDigestResultsResultsStatus(str, Enum):
    r"""The status of this company's digest computation."""

    SUCCESS = "success"
    PARTIAL_SUCCESS = "partial_success"
    FAILED = "failed"


class BlockersTypedDict(TypedDict):
    type: NotRequired[str]
    r"""A machine-readable blocker key (e.g. `missing_bank_account`)."""
    description: NotRequired[str]
    r"""Human-readable description of the blocker."""


class Blockers(BaseModel):
    type: Optional[str] = None
    r"""A machine-readable blocker key (e.g. `missing_bank_account`)."""

    description: Optional[str] = None
    r"""Human-readable description of the blocker."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["type", "description"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PayrollDigestResultsPayPeriodTypedDict(TypedDict):
    start_date: NotRequired[Nullable[date]]
    r"""First day of the pay period."""
    end_date: NotRequired[Nullable[date]]
    r"""Last day of the pay period."""
    check_date: NotRequired[Nullable[date]]
    r"""The date employees get paid."""
    run_payroll_by: NotRequired[Nullable[date]]
    r"""The deadline to run payroll for this pay period."""


class PayrollDigestResultsPayPeriod(BaseModel):
    start_date: OptionalNullable[date] = UNSET
    r"""First day of the pay period."""

    end_date: OptionalNullable[date] = UNSET
    r"""Last day of the pay period."""

    check_date: OptionalNullable[date] = UNSET
    r"""The date employees get paid."""

    run_payroll_by: OptionalNullable[date] = UNSET
    r"""The deadline to run payroll for this pay period."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["start_date", "end_date", "check_date", "run_payroll_by"]
        )
        nullable_fields = set(
            ["start_date", "end_date", "check_date", "run_payroll_by"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class PayScheduleTypedDict(TypedDict):
    uuid: NotRequired[str]
    r"""UUID of the pay schedule."""
    frequency: NotRequired[str]
    r"""Human-friendly pay frequency (e.g. \"Every other week\")."""
    custom_name: NotRequired[Nullable[str]]
    r"""Custom name for the pay schedule, when set."""


class PaySchedule(BaseModel):
    uuid: Optional[str] = None
    r"""UUID of the pay schedule."""

    frequency: Optional[str] = None
    r"""Human-friendly pay frequency (e.g. \"Every other week\")."""

    custom_name: OptionalNullable[str] = UNSET
    r"""Custom name for the pay schedule, when set."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["uuid", "frequency", "custom_name"])
        nullable_fields = set(["custom_name"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class PayrollDigestResultsTotalsTypedDict(TypedDict):
    r"""Pay totals. `null` when the payroll has not been calculated, or when the calculation is stale (the partner edited hours/earnings after the last calculation)."""

    total_debit_amount: NotRequired[str]
    r"""Total amount debited from the company bank account (string-formatted decimal)."""
    net_pay: NotRequired[str]
    r"""Total net pay across all employees on this payroll (string-formatted decimal)."""
    total_employer_cost: NotRequired[str]
    r"""Total employer cost including taxes and benefits (string-formatted decimal)."""


class PayrollDigestResultsTotals(BaseModel):
    r"""Pay totals. `null` when the payroll has not been calculated, or when the calculation is stale (the partner edited hours/earnings after the last calculation)."""

    total_debit_amount: Optional[str] = None
    r"""Total amount debited from the company bank account (string-formatted decimal)."""

    net_pay: Optional[str] = None
    r"""Total net pay across all employees on this payroll (string-formatted decimal)."""

    total_employer_cost: Optional[str] = None
    r"""Total employer cost including taxes and benefits (string-formatted decimal)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["total_debit_amount", "net_pay", "total_employer_cost"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PayrollsModelTypedDict(TypedDict):
    payroll_uuid: NotRequired[Nullable[str]]
    r"""UUID of the payroll. `null` for upcoming pay periods that have not been started yet (the `payrolls` API has not yet created a payroll record). Once a payroll is created, subsequent digest requests will include the real `payroll_uuid`."""
    payroll_type: NotRequired[str]
    r"""The type of payroll (e.g. `regular`, `new_hire`, `termination`, `transition`, `bonus`, `correction`)."""
    display_title: NotRequired[str]
    r"""Partner-facing display title for this payroll (e.g. \"Run biweekly payroll\")."""
    auto_payroll: NotRequired[bool]
    r"""Whether the company has auto-payroll enabled for this pay schedule."""
    status: NotRequired[str]
    r"""The lifecycle status of the payroll (e.g. `ready_to_start`, `in_progress`, `submitted`, `completed`, `failed`)."""
    pay_period: NotRequired[PayrollDigestResultsPayPeriodTypedDict]
    pay_schedule: NotRequired[Nullable[PayScheduleTypedDict]]
    totals: NotRequired[Nullable[PayrollDigestResultsTotalsTypedDict]]
    r"""Pay totals. `null` when the payroll has not been calculated, or when the calculation is stale (the partner edited hours/earnings after the last calculation)."""


class PayrollsModel(BaseModel):
    payroll_uuid: OptionalNullable[str] = UNSET
    r"""UUID of the payroll. `null` for upcoming pay periods that have not been started yet (the `payrolls` API has not yet created a payroll record). Once a payroll is created, subsequent digest requests will include the real `payroll_uuid`."""

    payroll_type: Optional[str] = None
    r"""The type of payroll (e.g. `regular`, `new_hire`, `termination`, `transition`, `bonus`, `correction`)."""

    display_title: Optional[str] = None
    r"""Partner-facing display title for this payroll (e.g. \"Run biweekly payroll\")."""

    auto_payroll: Optional[bool] = None
    r"""Whether the company has auto-payroll enabled for this pay schedule."""

    status: Optional[str] = None
    r"""The lifecycle status of the payroll (e.g. `ready_to_start`, `in_progress`, `submitted`, `completed`, `failed`)."""

    pay_period: Optional[PayrollDigestResultsPayPeriod] = None

    pay_schedule: OptionalNullable[PaySchedule] = UNSET

    totals: OptionalNullable[PayrollDigestResultsTotals] = UNSET
    r"""Pay totals. `null` when the payroll has not been calculated, or when the calculation is stale (the partner edited hours/earnings after the last calculation)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "payroll_uuid",
                "payroll_type",
                "display_title",
                "auto_payroll",
                "status",
                "pay_period",
                "pay_schedule",
                "totals",
            ]
        )
        nullable_fields = set(["payroll_uuid", "pay_schedule", "totals"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class PayrollDigestResultsResultsTypedDict(TypedDict):
    idx: NotRequired[int]
    r"""The index of this company in the original POST batch array."""
    entity_type: NotRequired[PayrollDigestResultsEntityType]
    r"""The type of entity this result represents."""
    uuid: NotRequired[str]
    r"""The UUID of the company."""
    name: NotRequired[str]
    r"""The legal/display name of the company."""
    status: NotRequired[PayrollDigestResultsResultsStatus]
    r"""The status of this company's digest computation."""
    blockers: NotRequired[List[BlockersTypedDict]]
    r"""Reasons the company cannot currently run payroll. Applies to every payroll in this company's `payrolls` array — blockers are evaluated at the company level, not per payroll. Empty when there are no blockers."""
    payrolls: NotRequired[List[PayrollsModelTypedDict]]
    r"""Payrolls for this company within the digest date window (7 days past, 30–60 days future). May be empty."""


class PayrollDigestResultsResults(BaseModel):
    idx: Optional[int] = None
    r"""The index of this company in the original POST batch array."""

    entity_type: Optional[PayrollDigestResultsEntityType] = None
    r"""The type of entity this result represents."""

    uuid: Optional[str] = None
    r"""The UUID of the company."""

    name: Optional[str] = None
    r"""The legal/display name of the company."""

    status: Optional[PayrollDigestResultsResultsStatus] = None
    r"""The status of this company's digest computation."""

    blockers: Optional[List[Blockers]] = None
    r"""Reasons the company cannot currently run payroll. Applies to every payroll in this company's `payrolls` array — blockers are evaluated at the company level, not per payroll. Empty when there are no blockers."""

    payrolls: Optional[List[PayrollsModel]] = None
    r"""Payrolls for this company within the digest date window (7 days past, 30–60 days future). May be empty."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["idx", "entity_type", "uuid", "name", "status", "blockers", "payrolls"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PayrollDigestResultsExclusionsEntityType(str, Enum):
    r"""The type of entity this exclusion represents."""

    COMPANY = "company"


class PayrollDigestResultsExclusionsStatus(str, Enum):
    r"""The status of this company's digest computation."""

    FAILED = "failed"


class PayrollDigestResultsCategory(str, Enum):
    r"""Machine-readable category for why the company was excluded."""

    NOT_FOUND = "not_found"
    COMPANY_INACTIVE = "company_inactive"
    DUPLICATE = "duplicate"
    INTERNAL_ERROR = "internal_error"


class PayrollDigestResultsExclusionsTypedDict(TypedDict):
    idx: NotRequired[int]
    r"""The index of this company in the original POST batch array."""
    entity_type: NotRequired[PayrollDigestResultsExclusionsEntityType]
    r"""The type of entity this exclusion represents."""
    uuid: NotRequired[str]
    r"""The UUID of the excluded company."""
    status: NotRequired[PayrollDigestResultsExclusionsStatus]
    r"""The status of this company's digest computation."""
    category: NotRequired[PayrollDigestResultsCategory]
    r"""Machine-readable category for why the company was excluded."""
    message: NotRequired[str]
    r"""Human-readable explanation for the exclusion."""


class PayrollDigestResultsExclusions(BaseModel):
    idx: Optional[int] = None
    r"""The index of this company in the original POST batch array."""

    entity_type: Optional[PayrollDigestResultsExclusionsEntityType] = None
    r"""The type of entity this exclusion represents."""

    uuid: Optional[str] = None
    r"""The UUID of the excluded company."""

    status: Optional[PayrollDigestResultsExclusionsStatus] = None
    r"""The status of this company's digest computation."""

    category: Optional[PayrollDigestResultsCategory] = None
    r"""Machine-readable category for why the company was excluded."""

    message: Optional[str] = None
    r"""Human-readable explanation for the exclusion."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["idx", "entity_type", "uuid", "status", "category", "message"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class PayrollDigestResultsTypedDict(TypedDict):
    r"""A payroll digest batch with processing results."""

    uuid: str
    r"""The unique identifier of the payroll digest batch."""
    idempotency_key: str
    r"""The idempotency key provided when creating the batch."""
    status: PayrollDigestResultsStatus
    r"""The lifecycle status of the batch request itself. Terminal values are `completed` (processing finished — inspect `results` and `exclusions` for per-company outcomes) and `failed` (request failed; can be retried). This is distinct from the per-company `status` returned inside `results[]` and `exclusions[]`."""
    submitted_at: datetime
    r"""The timestamp when the batch was submitted."""
    completed_at: NotRequired[Nullable[datetime]]
    r"""The timestamp when the batch processing completed."""
    submitted_items: NotRequired[Nullable[int]]
    r"""The number of companies submitted in the batch."""
    processed_items: NotRequired[int]
    r"""The number of companies successfully processed. Only present once the batch reaches a terminal status."""
    excluded_items: NotRequired[int]
    r"""The number of companies excluded from processing. Only present once the batch reaches a terminal status."""
    results: NotRequired[List[PayrollDigestResultsResultsTypedDict]]
    r"""Per-company results. Only present once the batch reaches a terminal status. Includes successfully processed companies (with their `payrolls` array, which may be empty when the company has no payrolls in the date window)."""
    exclusions: NotRequired[List[PayrollDigestResultsExclusionsTypedDict]]
    r"""Companies that could not be processed. Only present once the batch reaches a terminal status. Every UUID submitted in the POST batch appears in exactly one of `results` or `exclusions`."""


class PayrollDigestResults(BaseModel):
    r"""A payroll digest batch with processing results."""

    uuid: str
    r"""The unique identifier of the payroll digest batch."""

    idempotency_key: str
    r"""The idempotency key provided when creating the batch."""

    status: PayrollDigestResultsStatus
    r"""The lifecycle status of the batch request itself. Terminal values are `completed` (processing finished — inspect `results` and `exclusions` for per-company outcomes) and `failed` (request failed; can be retried). This is distinct from the per-company `status` returned inside `results[]` and `exclusions[]`."""

    submitted_at: datetime
    r"""The timestamp when the batch was submitted."""

    completed_at: OptionalNullable[datetime] = UNSET
    r"""The timestamp when the batch processing completed."""

    submitted_items: OptionalNullable[int] = UNSET
    r"""The number of companies submitted in the batch."""

    processed_items: Optional[int] = None
    r"""The number of companies successfully processed. Only present once the batch reaches a terminal status."""

    excluded_items: Optional[int] = None
    r"""The number of companies excluded from processing. Only present once the batch reaches a terminal status."""

    results: Optional[List[PayrollDigestResultsResults]] = None
    r"""Per-company results. Only present once the batch reaches a terminal status. Includes successfully processed companies (with their `payrolls` array, which may be empty when the company has no payrolls in the date window)."""

    exclusions: Optional[List[PayrollDigestResultsExclusions]] = None
    r"""Companies that could not be processed. Only present once the batch reaches a terminal status. Every UUID submitted in the POST batch appears in exactly one of `results` or `exclusions`."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "completed_at",
                "submitted_items",
                "processed_items",
                "excluded_items",
                "results",
                "exclusions",
            ]
        )
        nullable_fields = set(["completed_at", "submitted_items"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
