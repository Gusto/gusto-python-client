"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from gusto_embedded.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class GeneralLedgerReportAggregation(str, Enum):
    r"""The breakdown level used for the report."""

    DEFAULT = "default"
    JOB = "job"
    DEPARTMENT = "department"
    INTEGRATION = "integration"


class GeneralLedgerReportTypedDict(TypedDict):
    r"""A request for a general ledger report. The report is generated asynchronously and the URL is available via the report GET endpoint using the returned `request_uuid`."""

    payroll_uuid: NotRequired[str]
    r"""The UUID of the payroll record for which the report was generated."""
    aggregation: NotRequired[GeneralLedgerReportAggregation]
    r"""The breakdown level used for the report."""
    integration_type: NotRequired[Nullable[str]]
    r"""The `integration_type` used for the report when `aggregation` is 'integration' (e.g., `xero`, `qbo`). Otherwise, this will be null or an empty string."""
    request_uuid: NotRequired[str]
    r"""UUID to use for polling the report status."""


class GeneralLedgerReport(BaseModel):
    r"""A request for a general ledger report. The report is generated asynchronously and the URL is available via the report GET endpoint using the returned `request_uuid`."""

    payroll_uuid: Optional[str] = None
    r"""The UUID of the payroll record for which the report was generated."""

    aggregation: Optional[GeneralLedgerReportAggregation] = None
    r"""The breakdown level used for the report."""

    integration_type: OptionalNullable[str] = UNSET
    r"""The `integration_type` used for the report when `aggregation` is 'integration' (e.g., `xero`, `qbo`). Otherwise, this will be null or an empty string."""

    request_uuid: Optional[str] = None
    r"""UUID to use for polling the report status."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["payroll_uuid", "aggregation", "integration_type", "request_uuid"]
        )
        nullable_fields = set(["integration_type"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
