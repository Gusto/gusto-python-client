"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from gusto_embedded.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class Aggregation(str, Enum):
    r"""The breakdown level used for the report"""

    DEFAULT = "default"
    JOB = "job"
    DEPARTMENT = "department"
    INTEGRATION = "integration"


class IntegrationType(str, Enum):
    r"""The `integration_type` used for the report, if `aggregation` was 'integration.' Otherwise, this will be null."""

    XERO = "xero"
    QBO = "qbo"


class GeneralLedgerReportTypedDict(TypedDict):
    r"""Successful response for general ledger report generation"""

    payroll_uuid: NotRequired[str]
    r"""The UUID of the payroll record for which the report was generated"""
    aggregation: NotRequired[Aggregation]
    r"""The breakdown level used for the report"""
    integration_type: NotRequired[Nullable[IntegrationType]]
    r"""The `integration_type` used for the report, if `aggregation` was 'integration.' Otherwise, this will be null."""
    request_uuid: NotRequired[str]
    r"""UUID to use for polling the report status"""


class GeneralLedgerReport(BaseModel):
    r"""Successful response for general ledger report generation"""

    payroll_uuid: Optional[str] = None
    r"""The UUID of the payroll record for which the report was generated"""

    aggregation: Optional[Aggregation] = None
    r"""The breakdown level used for the report"""

    integration_type: OptionalNullable[IntegrationType] = UNSET
    r"""The `integration_type` used for the report, if `aggregation` was 'integration.' Otherwise, this will be null."""

    request_uuid: Optional[str] = None
    r"""UUID to use for polling the report status"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["payroll_uuid", "aggregation", "integration_type", "request_uuid"]
        )
        nullable_fields = set(["integration_type"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
