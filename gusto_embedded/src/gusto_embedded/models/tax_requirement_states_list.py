"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from gusto_embedded.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class SetupStatus(str, Enum):
    r"""The current status of the state tax setup.
    - `not_started`: No requirements have been filled
    - `in_progress`: Some requirements have been filled, or default rates are applied
    - `complete`: All requirements have been filled without default rates

    """

    NOT_STARTED = "not_started"
    IN_PROGRESS = "in_progress"
    COMPLETE = "complete"


class TaxRequirementStatesListTypedDict(TypedDict):
    state: NotRequired[str]
    r"""One of the two-letter state abbreviations for the fifty United States and the District of Columbia (DC)"""
    setup_complete: NotRequired[bool]
    r"""Whether all requirements for the state have been satisfied such that the company can complete onboarding. A company must be `setup_complete` in all relevant states to complete the `state_setup` company onboarding step."""
    setup_status: NotRequired[SetupStatus]
    r"""The current status of the state tax setup.
    - `not_started`: No requirements have been filled
    - `in_progress`: Some requirements have been filled, or default rates are applied
    - `complete`: All requirements have been filled without default rates

    """
    default_rates_applied: NotRequired[bool]
    r"""Whether the state is using system-assigned default SUI rates rather than employer-specific rates."""
    ready_to_run_payroll: NotRequired[bool]
    r"""Whether the state tax setup is sufficiently complete for the company to run payroll. This will be `true` when `setup_complete` is `true`."""


class TaxRequirementStatesList(BaseModel):
    state: Optional[str] = None
    r"""One of the two-letter state abbreviations for the fifty United States and the District of Columbia (DC)"""

    setup_complete: Optional[bool] = None
    r"""Whether all requirements for the state have been satisfied such that the company can complete onboarding. A company must be `setup_complete` in all relevant states to complete the `state_setup` company onboarding step."""

    setup_status: Optional[SetupStatus] = None
    r"""The current status of the state tax setup.
    - `not_started`: No requirements have been filled
    - `in_progress`: Some requirements have been filled, or default rates are applied
    - `complete`: All requirements have been filled without default rates

    """

    default_rates_applied: Optional[bool] = None
    r"""Whether the state is using system-assigned default SUI rates rather than employer-specific rates."""

    ready_to_run_payroll: Optional[bool] = None
    r"""Whether the state tax setup is sufficiently complete for the company to run payroll. This will be `true` when `setup_complete` is `true`."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "state",
                "setup_complete",
                "setup_status",
                "default_rates_applied",
                "ready_to_run_payroll",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
