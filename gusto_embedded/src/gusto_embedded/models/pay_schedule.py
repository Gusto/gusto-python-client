"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .pay_schedule_auto_payroll_enablement_blocker import (
    PayScheduleAutoPayrollEnablementBlocker,
    PayScheduleAutoPayrollEnablementBlockerTypedDict,
)
from .pay_schedule_frequency import PayScheduleFrequency
from datetime import date
from gusto_embedded.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class PayScheduleTypedDict(TypedDict):
    r"""The pay schedule object captures the details of when employees work and when they should be paid. A company can have multiple pay schedules.

    Response includes frequency, anchor dates, optional day_1/day_2 for monthly/semi-monthly, auto_pilot (or auto_payroll for API version 2025-11-15 and later), and auto_payroll_enablement_blockers when Autopayroll is disabled.

    **Webhooks:** Subscribe to [Pay Schedule Events](https://docs.gusto.com/embedded-payroll/docs/pay-schedule-events) to receive `pay_schedule.created` and `pay_schedule.updated` when pay schedules are created or updated.

    """

    uuid: str
    r"""The unique identifier of the pay schedule."""
    version: NotRequired[str]
    r"""The current version of the pay schedule. See the [versioning guide](https://docs.gusto.com/embedded-payroll/docs/api-fundamentals#optimistic-version-control) for information on how to use this field for optimistic concurrency."""
    frequency: NotRequired[PayScheduleFrequency]
    r"""Pay frequency for this schedule. READ-ONLY.

    - `Every week`: Employees are paid weekly.
    - `Every other week`: Employees are paid bi-weekly (every two weeks).
    - `Twice per month`: Employees are paid on two fixed days each month (e.g. 1st and 15th); use day_1 and day_2.
    - `Monthly`: Employees are paid once per month; use day_1 for the pay day.
    - `Quarterly`: Employees are paid every three months.
    - `Annually`: Employees are paid once per year.

    """
    anchor_pay_date: NotRequired[date]
    r"""The first date that employees on this pay schedule are paid (ISO 8601 YYYY-MM-DD)."""
    anchor_end_of_pay_period: NotRequired[date]
    r"""The last date of the first pay period. This can be the same date as the anchor pay date (ISO 8601 YYYY-MM-DD)."""
    day_1: NotRequired[Nullable[int]]
    r"""First pay day of the month (1-31).
    - **Twice per month, Monthly:** present (required on create/update).
    - **Every week, Every other week:** null.

    """
    day_2: NotRequired[Nullable[int]]
    r"""Second pay day of the month (1-31); only for **Twice per month**.
    - Use 31 for last day of month (shorter months use the actual last day).
    - **Other frequencies:** null.

    """
    name: NotRequired[Nullable[str]]
    r"""This field will be hourly when the pay schedule is for hourly employees, salaried when the pay schedule is for salaried employees, the department name if pay schedule is by department, and null when the pay schedule is for all employees."""
    custom_name: NotRequired[str]
    r"""Display name for the pay schedule. When the partner never set a custom name (or cleared it), this field contains the auto-generated description derived from frequency and pay days (e.g. \"every 1st and 15th of the month\", \"every Friday\"). When the partner set a custom name on create or update, this field contains that value.

    """
    auto_pilot: NotRequired[bool]
    r"""When true, autopilot is enabled and payroll runs automatically one day before payroll deadlines. When false, payroll does not run automatically.
    For API version 2025-11-15 and later the response uses auto_payroll (Autopayroll) instead.

    """
    auto_payroll: NotRequired[bool]
    r"""When true, Autopayroll is enabled and payroll runs automatically one day before payroll deadlines. When false, payroll does not run automatically.
    Returned for API version 2025-11-15 and later; for earlier versions the response uses auto_pilot instead.

    """
    active: NotRequired[bool]
    r"""Whether this pay schedule is associated with any employees. A pay schedule is inactive when it's unassigned."""
    auto_payroll_enablement_blockers: NotRequired[
        Nullable[List[PayScheduleAutoPayrollEnablementBlockerTypedDict]]
    ]
    r"""When auto_payroll is false, lists blockers that prevent enabling Autopayroll. When auto_payroll is true, this field is null."""


class PaySchedule(BaseModel):
    r"""The pay schedule object captures the details of when employees work and when they should be paid. A company can have multiple pay schedules.

    Response includes frequency, anchor dates, optional day_1/day_2 for monthly/semi-monthly, auto_pilot (or auto_payroll for API version 2025-11-15 and later), and auto_payroll_enablement_blockers when Autopayroll is disabled.

    **Webhooks:** Subscribe to [Pay Schedule Events](https://docs.gusto.com/embedded-payroll/docs/pay-schedule-events) to receive `pay_schedule.created` and `pay_schedule.updated` when pay schedules are created or updated.

    """

    uuid: str
    r"""The unique identifier of the pay schedule."""

    version: Optional[str] = None
    r"""The current version of the pay schedule. See the [versioning guide](https://docs.gusto.com/embedded-payroll/docs/api-fundamentals#optimistic-version-control) for information on how to use this field for optimistic concurrency."""

    frequency: Optional[PayScheduleFrequency] = None
    r"""Pay frequency for this schedule. READ-ONLY.

    - `Every week`: Employees are paid weekly.
    - `Every other week`: Employees are paid bi-weekly (every two weeks).
    - `Twice per month`: Employees are paid on two fixed days each month (e.g. 1st and 15th); use day_1 and day_2.
    - `Monthly`: Employees are paid once per month; use day_1 for the pay day.
    - `Quarterly`: Employees are paid every three months.
    - `Annually`: Employees are paid once per year.

    """

    anchor_pay_date: Optional[date] = None
    r"""The first date that employees on this pay schedule are paid (ISO 8601 YYYY-MM-DD)."""

    anchor_end_of_pay_period: Optional[date] = None
    r"""The last date of the first pay period. This can be the same date as the anchor pay date (ISO 8601 YYYY-MM-DD)."""

    day_1: OptionalNullable[int] = UNSET
    r"""First pay day of the month (1-31).
    - **Twice per month, Monthly:** present (required on create/update).
    - **Every week, Every other week:** null.

    """

    day_2: OptionalNullable[int] = UNSET
    r"""Second pay day of the month (1-31); only for **Twice per month**.
    - Use 31 for last day of month (shorter months use the actual last day).
    - **Other frequencies:** null.

    """

    name: OptionalNullable[str] = UNSET
    r"""This field will be hourly when the pay schedule is for hourly employees, salaried when the pay schedule is for salaried employees, the department name if pay schedule is by department, and null when the pay schedule is for all employees."""

    custom_name: Optional[str] = None
    r"""Display name for the pay schedule. When the partner never set a custom name (or cleared it), this field contains the auto-generated description derived from frequency and pay days (e.g. \"every 1st and 15th of the month\", \"every Friday\"). When the partner set a custom name on create or update, this field contains that value.

    """

    auto_pilot: Optional[bool] = None
    r"""When true, autopilot is enabled and payroll runs automatically one day before payroll deadlines. When false, payroll does not run automatically.
    For API version 2025-11-15 and later the response uses auto_payroll (Autopayroll) instead.

    """

    auto_payroll: Optional[bool] = None
    r"""When true, Autopayroll is enabled and payroll runs automatically one day before payroll deadlines. When false, payroll does not run automatically.
    Returned for API version 2025-11-15 and later; for earlier versions the response uses auto_pilot instead.

    """

    active: Optional[bool] = None
    r"""Whether this pay schedule is associated with any employees. A pay schedule is inactive when it's unassigned."""

    auto_payroll_enablement_blockers: OptionalNullable[
        List[PayScheduleAutoPayrollEnablementBlocker]
    ] = UNSET
    r"""When auto_payroll is false, lists blockers that prevent enabling Autopayroll. When auto_payroll is true, this field is null."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "version",
                "frequency",
                "anchor_pay_date",
                "anchor_end_of_pay_period",
                "day_1",
                "day_2",
                "name",
                "custom_name",
                "auto_pilot",
                "auto_payroll",
                "active",
                "auto_payroll_enablement_blockers",
            ]
        )
        nullable_fields = set(
            ["day_1", "day_2", "name", "auto_payroll_enablement_blockers"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
