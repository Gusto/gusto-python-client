"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from gusto_embedded.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class DetailedPaymentAccountSplitTypedDict(TypedDict):
    r"""Details of a single payment split for a payment method."""

    bank_account_uuid: NotRequired[str]
    r"""The UUID of the bank account."""
    hidden_account_number: NotRequired[str]
    r"""The masked account number."""
    name: NotRequired[str]
    r"""The name of the bank account."""
    priority: NotRequired[int]
    r"""The priority of the payment split."""
    split_amount: NotRequired[Nullable[int]]
    r"""If `split_by` is 'Amount', this is in cents (e.g., 500 for $5.00) and exactly one account must have a `split_amount` of `null` to capture the remainder. If `split_by` is 'Percentage', this is the percentage value (e.g., 60 for 60%)."""
    encrypted_account_number: NotRequired[Nullable[str]]
    r"""Ciphertext containing the full bank account number, which must be decrypted using a key provided by Gusto. Only visible with the appropriate `read:account_number` scope (e.g., `employee_payment_methods:read:account_number`)."""


class DetailedPaymentAccountSplit(BaseModel):
    r"""Details of a single payment split for a payment method."""

    bank_account_uuid: Optional[str] = None
    r"""The UUID of the bank account."""

    hidden_account_number: Optional[str] = None
    r"""The masked account number."""

    name: Optional[str] = None
    r"""The name of the bank account."""

    priority: Optional[int] = None
    r"""The priority of the payment split."""

    split_amount: OptionalNullable[int] = UNSET
    r"""If `split_by` is 'Amount', this is in cents (e.g., 500 for $5.00) and exactly one account must have a `split_amount` of `null` to capture the remainder. If `split_by` is 'Percentage', this is the percentage value (e.g., 60 for 60%)."""

    encrypted_account_number: OptionalNullable[str] = UNSET
    r"""Ciphertext containing the full bank account number, which must be decrypted using a key provided by Gusto. Only visible with the appropriate `read:account_number` scope (e.g., `employee_payment_methods:read:account_number`)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "bank_account_uuid",
                "hidden_account_number",
                "name",
                "priority",
                "split_amount",
                "encrypted_account_number",
            ]
        )
        nullable_fields = set(["split_amount", "encrypted_account_number"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
