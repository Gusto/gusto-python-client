"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from datetime import date
from gusto_embedded import models, utils
from gusto_embedded._hooks import HookContext
from gusto_embedded.types import Nullable, OptionalNullable, UNSET
from gusto_embedded.utils import get_security_from_env
from gusto_embedded.utils.unmarshal_json_response import unmarshal_json_response
from typing import Any, List, Mapping, Optional, Union


class PaySchedules(BaseSDK):
    def create(
        self,
        *,
        company_id: str,
        frequency: models.PayScheduleFrequencyCreateUpdate,
        anchor_pay_date: date,
        anchor_end_of_pay_period: date,
        x_gusto_api_version: Optional[
            models.PostV1CompaniesCompanyIDPaySchedulesHeaderXGustoAPIVersion
        ] = models.PostV1CompaniesCompanyIDPaySchedulesHeaderXGustoAPIVersion.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        day_1: OptionalNullable[int] = UNSET,
        day_2: OptionalNullable[int] = UNSET,
        custom_name: OptionalNullable[str] = UNSET,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.PaySchedule:
        r"""Create a new pay schedule

        If a company does not have any pay schedules, this endpoint creates a single pay schedule and assigns it to all employees (common during company onboarding).

        If a company already has an active pay schedule and wants multiple pay schedules, this endpoint creates a pay schedule that is not assigned to any employee.

        Be sure to [check state laws](https://www.dol.gov/agencies/whd/state/payday) to know what schedule is right for your customers. If an onboarded company misses their first pay date, the pay schedule may be automatically adjusted.

        ### Webhooks
        - `pay_schedule.created`: Fires when a pay schedule is successfully created.

        ### Related guides
        - [Create a pay schedule](doc:create-a-pay-schedule)
        - [Pay Schedules](doc:pay-schedule-info)
        - [Manage Pay Schedules via API](doc:manage-pay-schedules-api)

        scope: `pay_schedules:write`

        :param company_id: The UUID of the company
        :param frequency: Pay frequency when creating or updating a schedule. Only weekly, bi-weekly, twice per month, and monthly are supported via the API.

            - `Every week`: Weekly pay.
            - `Every other week`: Biweekly pay.
            - `Twice per month`: Two pay dates per month; require day_1 and day_2 (use 31 for last day of month).
            - `Monthly`: One pay date per month; require day_1 (1-31).

        :param anchor_pay_date: ISO 8601 date (YYYY-MM-DD). Required for anchor and period dates in create, update, and preview requests.
        :param anchor_end_of_pay_period: ISO 8601 date (YYYY-MM-DD). Required for anchor and period dates in create, update, and preview requests.
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param day_1: First pay day of the month (1-31).
            - **Twice per month, Monthly:** required.
            - **Every week, Every other week:** omit or null.

        :param day_2: Second pay day of the month (1-31); only for **Twice per month**.
            - Use 31 for last day of month (shorter months use the actual last day).
            - **Other frequencies:** omit or null.

        :param custom_name: Optional display name for the pay schedule.

            When null or omitted, the system generates a description from the pay frequency and pay days (e.g. \"every 1st and 15th of the month\" for twice-monthly, \"every 11th of the month\" for monthly, \"every Friday\" for weekly). The response returns this generated value in `custom_name` when no custom name was set. When provided, the value you set is stored and returned.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.PostV1CompaniesCompanyIDPaySchedulesRequest(
            x_gusto_api_version=x_gusto_api_version,
            company_id=company_id,
            pay_schedule_create_request=models.PayScheduleCreateRequest(
                frequency=frequency,
                anchor_pay_date=anchor_pay_date,
                anchor_end_of_pay_period=anchor_end_of_pay_period,
                day_1=day_1,
                day_2=day_2,
                custom_name=custom_name,
            ),
        )

        req = self._build_request(
            method="POST",
            path="/v1/companies/{company_id}/pay_schedules",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.pay_schedule_create_request,
                False,
                False,
                "json",
                models.PayScheduleCreateRequest,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="post-v1-companies-company_id-pay_schedules",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["404", "422", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "201", "application/json"):
            return unmarshal_json_response(models.PaySchedule, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                models.NotFoundErrorObjectData, http_res
            )
            raise models.NotFoundErrorObject(response_data, http_res)
        if utils.match_response(http_res, "422", "application/json"):
            response_data = unmarshal_json_response(
                models.UnprocessableEntityErrorObjectData, http_res
            )
            raise models.UnprocessableEntityErrorObject(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    async def create_async(
        self,
        *,
        company_id: str,
        frequency: models.PayScheduleFrequencyCreateUpdate,
        anchor_pay_date: date,
        anchor_end_of_pay_period: date,
        x_gusto_api_version: Optional[
            models.PostV1CompaniesCompanyIDPaySchedulesHeaderXGustoAPIVersion
        ] = models.PostV1CompaniesCompanyIDPaySchedulesHeaderXGustoAPIVersion.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        day_1: OptionalNullable[int] = UNSET,
        day_2: OptionalNullable[int] = UNSET,
        custom_name: OptionalNullable[str] = UNSET,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.PaySchedule:
        r"""Create a new pay schedule

        If a company does not have any pay schedules, this endpoint creates a single pay schedule and assigns it to all employees (common during company onboarding).

        If a company already has an active pay schedule and wants multiple pay schedules, this endpoint creates a pay schedule that is not assigned to any employee.

        Be sure to [check state laws](https://www.dol.gov/agencies/whd/state/payday) to know what schedule is right for your customers. If an onboarded company misses their first pay date, the pay schedule may be automatically adjusted.

        ### Webhooks
        - `pay_schedule.created`: Fires when a pay schedule is successfully created.

        ### Related guides
        - [Create a pay schedule](doc:create-a-pay-schedule)
        - [Pay Schedules](doc:pay-schedule-info)
        - [Manage Pay Schedules via API](doc:manage-pay-schedules-api)

        scope: `pay_schedules:write`

        :param company_id: The UUID of the company
        :param frequency: Pay frequency when creating or updating a schedule. Only weekly, bi-weekly, twice per month, and monthly are supported via the API.

            - `Every week`: Weekly pay.
            - `Every other week`: Biweekly pay.
            - `Twice per month`: Two pay dates per month; require day_1 and day_2 (use 31 for last day of month).
            - `Monthly`: One pay date per month; require day_1 (1-31).

        :param anchor_pay_date: ISO 8601 date (YYYY-MM-DD). Required for anchor and period dates in create, update, and preview requests.
        :param anchor_end_of_pay_period: ISO 8601 date (YYYY-MM-DD). Required for anchor and period dates in create, update, and preview requests.
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param day_1: First pay day of the month (1-31).
            - **Twice per month, Monthly:** required.
            - **Every week, Every other week:** omit or null.

        :param day_2: Second pay day of the month (1-31); only for **Twice per month**.
            - Use 31 for last day of month (shorter months use the actual last day).
            - **Other frequencies:** omit or null.

        :param custom_name: Optional display name for the pay schedule.

            When null or omitted, the system generates a description from the pay frequency and pay days (e.g. \"every 1st and 15th of the month\" for twice-monthly, \"every 11th of the month\" for monthly, \"every Friday\" for weekly). The response returns this generated value in `custom_name` when no custom name was set. When provided, the value you set is stored and returned.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.PostV1CompaniesCompanyIDPaySchedulesRequest(
            x_gusto_api_version=x_gusto_api_version,
            company_id=company_id,
            pay_schedule_create_request=models.PayScheduleCreateRequest(
                frequency=frequency,
                anchor_pay_date=anchor_pay_date,
                anchor_end_of_pay_period=anchor_end_of_pay_period,
                day_1=day_1,
                day_2=day_2,
                custom_name=custom_name,
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/v1/companies/{company_id}/pay_schedules",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.pay_schedule_create_request,
                False,
                False,
                "json",
                models.PayScheduleCreateRequest,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="post-v1-companies-company_id-pay_schedules",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["404", "422", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "201", "application/json"):
            return unmarshal_json_response(models.PaySchedule, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                models.NotFoundErrorObjectData, http_res
            )
            raise models.NotFoundErrorObject(response_data, http_res)
        if utils.match_response(http_res, "422", "application/json"):
            response_data = unmarshal_json_response(
                models.UnprocessableEntityErrorObjectData, http_res
            )
            raise models.UnprocessableEntityErrorObject(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    def get_all(
        self,
        *,
        company_id: str,
        x_gusto_api_version: Optional[
            models.GetV1CompaniesCompanyIDPaySchedulesHeaderXGustoAPIVersion
        ] = models.GetV1CompaniesCompanyIDPaySchedulesHeaderXGustoAPIVersion.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        page: Optional[int] = None,
        per: Optional[int] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> List[models.PaySchedule]:
        r"""Get the pay schedules for a company

        Returns all pay schedules for a company. The pay schedule object captures the details of when employees work and when they should be paid. A company can have multiple pay schedules.

        scope: `pay_schedules:read`

        :param company_id: The UUID of the company
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param page: The page that is requested. When unspecified, will load all objects unless endpoint forces pagination.
        :param per: Number of objects per page. For majority of endpoints will default to 25
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetV1CompaniesCompanyIDPaySchedulesRequest(
            x_gusto_api_version=x_gusto_api_version,
            company_id=company_id,
            page=page,
            per=per,
        )

        req = self._build_request(
            method="GET",
            path="/v1/companies/{company_id}/pay_schedules",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="get-v1-companies-company_id-pay_schedules",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(List[models.PaySchedule], http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                models.NotFoundErrorObjectData, http_res
            )
            raise models.NotFoundErrorObject(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    async def get_all_async(
        self,
        *,
        company_id: str,
        x_gusto_api_version: Optional[
            models.GetV1CompaniesCompanyIDPaySchedulesHeaderXGustoAPIVersion
        ] = models.GetV1CompaniesCompanyIDPaySchedulesHeaderXGustoAPIVersion.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        page: Optional[int] = None,
        per: Optional[int] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> List[models.PaySchedule]:
        r"""Get the pay schedules for a company

        Returns all pay schedules for a company. The pay schedule object captures the details of when employees work and when they should be paid. A company can have multiple pay schedules.

        scope: `pay_schedules:read`

        :param company_id: The UUID of the company
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param page: The page that is requested. When unspecified, will load all objects unless endpoint forces pagination.
        :param per: Number of objects per page. For majority of endpoints will default to 25
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetV1CompaniesCompanyIDPaySchedulesRequest(
            x_gusto_api_version=x_gusto_api_version,
            company_id=company_id,
            page=page,
            per=per,
        )

        req = self._build_request_async(
            method="GET",
            path="/v1/companies/{company_id}/pay_schedules",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="get-v1-companies-company_id-pay_schedules",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(List[models.PaySchedule], http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                models.NotFoundErrorObjectData, http_res
            )
            raise models.NotFoundErrorObject(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    def get_preview(
        self,
        *,
        company_id: str,
        frequency: models.Frequency,
        anchor_pay_date: date,
        anchor_end_of_pay_period: date,
        x_gusto_api_version: Optional[
            models.GetV1CompaniesCompanyIDPaySchedulesPreviewHeaderXGustoAPIVersion
        ] = models.GetV1CompaniesCompanyIDPaySchedulesPreviewHeaderXGustoAPIVersion.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        day_1: Optional[int] = None,
        day_2: Optional[int] = None,
        end_date: Optional[date] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.PaySchedulePreview:
        r"""Preview pay schedule dates

        Returns a preview of pay period dates and holidays for the given parameters (e.g. frequency, anchor pay date) for the next 18 months. Use this before creating or updating a pay schedule to show expected check dates and payroll deadlines.

        ### Related guides
        - [Create a pay schedule](doc:create-a-pay-schedule)
        - [Manage Pay Schedules via API](doc:manage-pay-schedules-api)

        scope: `pay_schedules:write`

        :param company_id: The UUID of the company
        :param frequency: The frequency that employees on this pay schedule are paid.
        :param anchor_pay_date: The first date that employees on this pay schedule are paid.
        :param anchor_end_of_pay_period: The last date of the first pay period. This can be the same date as the anchor pay date.
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param day_1: First pay day of the month (1-31).
            - **Twice per month, Monthly:** required.
            - **Every week, Every other week:** omit or null.
        :param day_2: Second pay day of the month (1-31); only for **Twice per month**.
            - Use 31 for last day of month (shorter months use the actual last day).
            - **Other frequencies:** omit or null.
        :param end_date: End date for the preview range. When unspecified, defaults to 18 months from today.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetV1CompaniesCompanyIDPaySchedulesPreviewRequest(
            x_gusto_api_version=x_gusto_api_version,
            company_id=company_id,
            frequency=frequency,
            anchor_pay_date=anchor_pay_date,
            anchor_end_of_pay_period=anchor_end_of_pay_period,
            day_1=day_1,
            day_2=day_2,
            end_date=end_date,
        )

        req = self._build_request(
            method="GET",
            path="/v1/companies/{company_id}/pay_schedules/preview",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="get-v1-companies-company_id-pay_schedules-preview",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["404", "422", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.PaySchedulePreview, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                models.NotFoundErrorObjectData, http_res
            )
            raise models.NotFoundErrorObject(response_data, http_res)
        if utils.match_response(http_res, "422", "application/json"):
            response_data = unmarshal_json_response(
                models.UnprocessableEntityErrorObjectData, http_res
            )
            raise models.UnprocessableEntityErrorObject(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    async def get_preview_async(
        self,
        *,
        company_id: str,
        frequency: models.Frequency,
        anchor_pay_date: date,
        anchor_end_of_pay_period: date,
        x_gusto_api_version: Optional[
            models.GetV1CompaniesCompanyIDPaySchedulesPreviewHeaderXGustoAPIVersion
        ] = models.GetV1CompaniesCompanyIDPaySchedulesPreviewHeaderXGustoAPIVersion.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        day_1: Optional[int] = None,
        day_2: Optional[int] = None,
        end_date: Optional[date] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.PaySchedulePreview:
        r"""Preview pay schedule dates

        Returns a preview of pay period dates and holidays for the given parameters (e.g. frequency, anchor pay date) for the next 18 months. Use this before creating or updating a pay schedule to show expected check dates and payroll deadlines.

        ### Related guides
        - [Create a pay schedule](doc:create-a-pay-schedule)
        - [Manage Pay Schedules via API](doc:manage-pay-schedules-api)

        scope: `pay_schedules:write`

        :param company_id: The UUID of the company
        :param frequency: The frequency that employees on this pay schedule are paid.
        :param anchor_pay_date: The first date that employees on this pay schedule are paid.
        :param anchor_end_of_pay_period: The last date of the first pay period. This can be the same date as the anchor pay date.
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param day_1: First pay day of the month (1-31).
            - **Twice per month, Monthly:** required.
            - **Every week, Every other week:** omit or null.
        :param day_2: Second pay day of the month (1-31); only for **Twice per month**.
            - Use 31 for last day of month (shorter months use the actual last day).
            - **Other frequencies:** omit or null.
        :param end_date: End date for the preview range. When unspecified, defaults to 18 months from today.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetV1CompaniesCompanyIDPaySchedulesPreviewRequest(
            x_gusto_api_version=x_gusto_api_version,
            company_id=company_id,
            frequency=frequency,
            anchor_pay_date=anchor_pay_date,
            anchor_end_of_pay_period=anchor_end_of_pay_period,
            day_1=day_1,
            day_2=day_2,
            end_date=end_date,
        )

        req = self._build_request_async(
            method="GET",
            path="/v1/companies/{company_id}/pay_schedules/preview",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="get-v1-companies-company_id-pay_schedules-preview",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["404", "422", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.PaySchedulePreview, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                models.NotFoundErrorObjectData, http_res
            )
            raise models.NotFoundErrorObject(response_data, http_res)
        if utils.match_response(http_res, "422", "application/json"):
            response_data = unmarshal_json_response(
                models.UnprocessableEntityErrorObjectData, http_res
            )
            raise models.UnprocessableEntityErrorObject(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    def get(
        self,
        *,
        company_id: str,
        pay_schedule_id: str,
        x_gusto_api_version: Optional[
            models.GetV1CompaniesCompanyIDPaySchedulesPayScheduleIDHeaderXGustoAPIVersion
        ] = models.GetV1CompaniesCompanyIDPaySchedulesPayScheduleIDHeaderXGustoAPIVersion.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.PaySchedule:
        r"""Get a pay schedule

        Returns a single pay schedule by UUID. The pay schedule object captures the details of when employees work and when they should be paid. A company can have multiple pay schedules.

        scope: `pay_schedules:read`

        :param company_id: The UUID of the company
        :param pay_schedule_id: The UUID of the pay schedule
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetV1CompaniesCompanyIDPaySchedulesPayScheduleIDRequest(
            x_gusto_api_version=x_gusto_api_version,
            company_id=company_id,
            pay_schedule_id=pay_schedule_id,
        )

        req = self._build_request(
            method="GET",
            path="/v1/companies/{company_id}/pay_schedules/{pay_schedule_id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="get-v1-companies-company_id-pay_schedules-pay_schedule_id",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.PaySchedule, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                models.NotFoundErrorObjectData, http_res
            )
            raise models.NotFoundErrorObject(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    async def get_async(
        self,
        *,
        company_id: str,
        pay_schedule_id: str,
        x_gusto_api_version: Optional[
            models.GetV1CompaniesCompanyIDPaySchedulesPayScheduleIDHeaderXGustoAPIVersion
        ] = models.GetV1CompaniesCompanyIDPaySchedulesPayScheduleIDHeaderXGustoAPIVersion.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.PaySchedule:
        r"""Get a pay schedule

        Returns a single pay schedule by UUID. The pay schedule object captures the details of when employees work and when they should be paid. A company can have multiple pay schedules.

        scope: `pay_schedules:read`

        :param company_id: The UUID of the company
        :param pay_schedule_id: The UUID of the pay schedule
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetV1CompaniesCompanyIDPaySchedulesPayScheduleIDRequest(
            x_gusto_api_version=x_gusto_api_version,
            company_id=company_id,
            pay_schedule_id=pay_schedule_id,
        )

        req = self._build_request_async(
            method="GET",
            path="/v1/companies/{company_id}/pay_schedules/{pay_schedule_id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="get-v1-companies-company_id-pay_schedules-pay_schedule_id",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.PaySchedule, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                models.NotFoundErrorObjectData, http_res
            )
            raise models.NotFoundErrorObject(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    def update(
        self,
        *,
        company_id: str,
        pay_schedule_id: str,
        version: str,
        x_gusto_api_version: Optional[
            models.PutV1CompaniesCompanyIDPaySchedulesPayScheduleIDHeaderXGustoAPIVersion
        ] = models.PutV1CompaniesCompanyIDPaySchedulesPayScheduleIDHeaderXGustoAPIVersion.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        auto_payroll: Optional[bool] = None,
        frequency: Optional[models.PayScheduleFrequencyCreateUpdate] = None,
        anchor_pay_date: Optional[date] = None,
        anchor_end_of_pay_period: Optional[date] = None,
        day_1: OptionalNullable[int] = UNSET,
        day_2: OptionalNullable[int] = UNSET,
        custom_name: OptionalNullable[str] = UNSET,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.PaySchedule:
        r"""Update a pay schedule

        Updates a pay schedule. The `version` parameter from the GET response is required for [optimistic concurrency](doc:api-fundamentals); a mismatch returns 409 Conflict.

        ### Pay schedules may be automatically adjusted
        If an onboarded company misses their first pay date, the pay schedule may be automatically adjusted.

        ### Webhooks
        - `pay_schedule.updated`: Fires when a pay schedule is successfully updated.

        ### Related guides
        - [Create a pay schedule](doc:create-a-pay-schedule)
        - [Manage Pay Schedules via API](doc:manage-pay-schedules-api)

        scope: `pay_schedules:write`

        :param company_id: The UUID of the company
        :param pay_schedule_id: The UUID of the pay schedule
        :param version: Current version of the pay schedule from the GET response; required for optimistic concurrency. Mismatch returns 409 Conflict.
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param auto_payroll: When true, Autopayroll is enabled and payroll runs automatically one day before payroll deadlines. When false, payroll does not run automatically and must be run manually.
            For API versions before 2025-11-15 the request field is auto_pilot.

        :param frequency: Pay frequency when creating or updating a schedule. Only weekly, bi-weekly, twice per month, and monthly are supported via the API.

            - `Every week`: Weekly pay.
            - `Every other week`: Biweekly pay.
            - `Twice per month`: Two pay dates per month; require day_1 and day_2 (use 31 for last day of month).
            - `Monthly`: One pay date per month; require day_1 (1-31).

        :param anchor_pay_date: ISO 8601 date (YYYY-MM-DD). Required for anchor and period dates in create, update, and preview requests.
        :param anchor_end_of_pay_period: ISO 8601 date (YYYY-MM-DD). Required for anchor and period dates in create, update, and preview requests.
        :param day_1: First pay day of the month (1–31). Required for Twice per month and Monthly; null for Every week and Every other week.

        :param day_2: Second pay day of the month (1–31); only for Twice per month. Use 31 for last day of month. Null for other frequencies.

        :param custom_name: Custom name for the pay schedule; null clears it.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.PutV1CompaniesCompanyIDPaySchedulesPayScheduleIDRequest(
            x_gusto_api_version=x_gusto_api_version,
            company_id=company_id,
            pay_schedule_id=pay_schedule_id,
            pay_schedule_update_request=models.PayScheduleUpdateRequest(
                version=version,
                auto_payroll=auto_payroll,
                frequency=frequency,
                anchor_pay_date=anchor_pay_date,
                anchor_end_of_pay_period=anchor_end_of_pay_period,
                day_1=day_1,
                day_2=day_2,
                custom_name=custom_name,
            ),
        )

        req = self._build_request(
            method="PUT",
            path="/v1/companies/{company_id}/pay_schedules/{pay_schedule_id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.pay_schedule_update_request,
                False,
                False,
                "json",
                models.PayScheduleUpdateRequest,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="put-v1-companies-company_id-pay_schedules-pay_schedule_id",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["404", "409", "422", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.PaySchedule, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                models.NotFoundErrorObjectData, http_res
            )
            raise models.NotFoundErrorObject(response_data, http_res)
        if utils.match_response(http_res, ["409", "422"], "application/json"):
            response_data = unmarshal_json_response(
                models.UnprocessableEntityErrorObjectData, http_res
            )
            raise models.UnprocessableEntityErrorObject(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    async def update_async(
        self,
        *,
        company_id: str,
        pay_schedule_id: str,
        version: str,
        x_gusto_api_version: Optional[
            models.PutV1CompaniesCompanyIDPaySchedulesPayScheduleIDHeaderXGustoAPIVersion
        ] = models.PutV1CompaniesCompanyIDPaySchedulesPayScheduleIDHeaderXGustoAPIVersion.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        auto_payroll: Optional[bool] = None,
        frequency: Optional[models.PayScheduleFrequencyCreateUpdate] = None,
        anchor_pay_date: Optional[date] = None,
        anchor_end_of_pay_period: Optional[date] = None,
        day_1: OptionalNullable[int] = UNSET,
        day_2: OptionalNullable[int] = UNSET,
        custom_name: OptionalNullable[str] = UNSET,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.PaySchedule:
        r"""Update a pay schedule

        Updates a pay schedule. The `version` parameter from the GET response is required for [optimistic concurrency](doc:api-fundamentals); a mismatch returns 409 Conflict.

        ### Pay schedules may be automatically adjusted
        If an onboarded company misses their first pay date, the pay schedule may be automatically adjusted.

        ### Webhooks
        - `pay_schedule.updated`: Fires when a pay schedule is successfully updated.

        ### Related guides
        - [Create a pay schedule](doc:create-a-pay-schedule)
        - [Manage Pay Schedules via API](doc:manage-pay-schedules-api)

        scope: `pay_schedules:write`

        :param company_id: The UUID of the company
        :param pay_schedule_id: The UUID of the pay schedule
        :param version: Current version of the pay schedule from the GET response; required for optimistic concurrency. Mismatch returns 409 Conflict.
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param auto_payroll: When true, Autopayroll is enabled and payroll runs automatically one day before payroll deadlines. When false, payroll does not run automatically and must be run manually.
            For API versions before 2025-11-15 the request field is auto_pilot.

        :param frequency: Pay frequency when creating or updating a schedule. Only weekly, bi-weekly, twice per month, and monthly are supported via the API.

            - `Every week`: Weekly pay.
            - `Every other week`: Biweekly pay.
            - `Twice per month`: Two pay dates per month; require day_1 and day_2 (use 31 for last day of month).
            - `Monthly`: One pay date per month; require day_1 (1-31).

        :param anchor_pay_date: ISO 8601 date (YYYY-MM-DD). Required for anchor and period dates in create, update, and preview requests.
        :param anchor_end_of_pay_period: ISO 8601 date (YYYY-MM-DD). Required for anchor and period dates in create, update, and preview requests.
        :param day_1: First pay day of the month (1–31). Required for Twice per month and Monthly; null for Every week and Every other week.

        :param day_2: Second pay day of the month (1–31); only for Twice per month. Use 31 for last day of month. Null for other frequencies.

        :param custom_name: Custom name for the pay schedule; null clears it.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.PutV1CompaniesCompanyIDPaySchedulesPayScheduleIDRequest(
            x_gusto_api_version=x_gusto_api_version,
            company_id=company_id,
            pay_schedule_id=pay_schedule_id,
            pay_schedule_update_request=models.PayScheduleUpdateRequest(
                version=version,
                auto_payroll=auto_payroll,
                frequency=frequency,
                anchor_pay_date=anchor_pay_date,
                anchor_end_of_pay_period=anchor_end_of_pay_period,
                day_1=day_1,
                day_2=day_2,
                custom_name=custom_name,
            ),
        )

        req = self._build_request_async(
            method="PUT",
            path="/v1/companies/{company_id}/pay_schedules/{pay_schedule_id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.pay_schedule_update_request,
                False,
                False,
                "json",
                models.PayScheduleUpdateRequest,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="put-v1-companies-company_id-pay_schedules-pay_schedule_id",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["404", "409", "422", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.PaySchedule, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                models.NotFoundErrorObjectData, http_res
            )
            raise models.NotFoundErrorObject(response_data, http_res)
        if utils.match_response(http_res, ["409", "422"], "application/json"):
            response_data = unmarshal_json_response(
                models.UnprocessableEntityErrorObjectData, http_res
            )
            raise models.UnprocessableEntityErrorObject(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    def get_pay_periods(
        self,
        *,
        company_id: str,
        x_gusto_api_version: Optional[
            models.GetV1CompaniesCompanyIDPayPeriodsHeaderXGustoAPIVersion
        ] = models.GetV1CompaniesCompanyIDPayPeriodsHeaderXGustoAPIVersion.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        start_date: Optional[date] = None,
        end_date: Optional[date] = None,
        payroll_types: Optional[models.PayrollTypes] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> List[models.PayPeriod]:
        r"""Get pay periods for a company

        Pay periods are the foundation of payroll. Compensation, time & attendance, taxes, and expense reports all rely on when they happened.

        To begin submitting information for a given payroll, we need to agree on the time period.

        By default, this endpoint returns pay periods starting from 6 months ago to the date today. Use the `start_date` and `end_date` parameters to change the scope of the response. End dates can be up to 3 months in the future and there is no limit on start dates.

        Starting in version 2023-04-01, the `eligible_employees` attribute was removed from the response. The eligible employees for a payroll are determined by the employee_compensations returned from the [PUT /v1/companies/{company_id}/payrolls/{payroll_id}/prepare](ref:put-v1-companies-company_id-payrolls-payroll_id-prepare) endpoint.

        scope: `payrolls:read`

        :param company_id: The UUID of the company
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param start_date: Start date (YYYY-MM-DD) for the pay periods range. Defaults to 6 months ago.
        :param end_date: End date (YYYY-MM-DD) for the pay periods range. Cannot be more than 3 months in the future. Defaults to today.
        :param payroll_types: Comma-separated list of payroll types to include (regular, transition). Defaults to regular only.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetV1CompaniesCompanyIDPayPeriodsRequest(
            x_gusto_api_version=x_gusto_api_version,
            company_id=company_id,
            start_date=start_date,
            end_date=end_date,
            payroll_types=payroll_types,
        )

        req = self._build_request(
            method="GET",
            path="/v1/companies/{company_id}/pay_periods",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="get-v1-companies-company_id-pay_periods",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["404", "422", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(List[models.PayPeriod], http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                models.NotFoundErrorObjectData, http_res
            )
            raise models.NotFoundErrorObject(response_data, http_res)
        if utils.match_response(http_res, "422", "application/json"):
            response_data = unmarshal_json_response(
                models.UnprocessableEntityErrorObjectData, http_res
            )
            raise models.UnprocessableEntityErrorObject(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    async def get_pay_periods_async(
        self,
        *,
        company_id: str,
        x_gusto_api_version: Optional[
            models.GetV1CompaniesCompanyIDPayPeriodsHeaderXGustoAPIVersion
        ] = models.GetV1CompaniesCompanyIDPayPeriodsHeaderXGustoAPIVersion.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        start_date: Optional[date] = None,
        end_date: Optional[date] = None,
        payroll_types: Optional[models.PayrollTypes] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> List[models.PayPeriod]:
        r"""Get pay periods for a company

        Pay periods are the foundation of payroll. Compensation, time & attendance, taxes, and expense reports all rely on when they happened.

        To begin submitting information for a given payroll, we need to agree on the time period.

        By default, this endpoint returns pay periods starting from 6 months ago to the date today. Use the `start_date` and `end_date` parameters to change the scope of the response. End dates can be up to 3 months in the future and there is no limit on start dates.

        Starting in version 2023-04-01, the `eligible_employees` attribute was removed from the response. The eligible employees for a payroll are determined by the employee_compensations returned from the [PUT /v1/companies/{company_id}/payrolls/{payroll_id}/prepare](ref:put-v1-companies-company_id-payrolls-payroll_id-prepare) endpoint.

        scope: `payrolls:read`

        :param company_id: The UUID of the company
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param start_date: Start date (YYYY-MM-DD) for the pay periods range. Defaults to 6 months ago.
        :param end_date: End date (YYYY-MM-DD) for the pay periods range. Cannot be more than 3 months in the future. Defaults to today.
        :param payroll_types: Comma-separated list of payroll types to include (regular, transition). Defaults to regular only.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetV1CompaniesCompanyIDPayPeriodsRequest(
            x_gusto_api_version=x_gusto_api_version,
            company_id=company_id,
            start_date=start_date,
            end_date=end_date,
            payroll_types=payroll_types,
        )

        req = self._build_request_async(
            method="GET",
            path="/v1/companies/{company_id}/pay_periods",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="get-v1-companies-company_id-pay_periods",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["404", "422", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(List[models.PayPeriod], http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                models.NotFoundErrorObjectData, http_res
            )
            raise models.NotFoundErrorObject(response_data, http_res)
        if utils.match_response(http_res, "422", "application/json"):
            response_data = unmarshal_json_response(
                models.UnprocessableEntityErrorObjectData, http_res
            )
            raise models.UnprocessableEntityErrorObject(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    def get_unprocessed_termination_periods(
        self,
        *,
        company_id: str,
        x_gusto_api_version: Optional[
            models.VersionHeader
        ] = models.VersionHeader.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> List[models.UnprocessedTerminationPayPeriod]:
        r"""Get termination pay periods for a company

        When a payroll admin terminates an employee and selects \"Dismissal Payroll\" as the employee's final payroll, their last pay period will appear on the list.

        This endpoint returns the unprocessed pay periods for past and future terminated employees in a given company.

        scope: `payrolls:read`

        :param company_id: The UUID of the company
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetV1CompaniesCompanyIDUnprocessedTerminationPayPeriodsRequest(
            company_id=company_id,
            x_gusto_api_version=x_gusto_api_version,
        )

        req = self._build_request(
            method="GET",
            path="/v1/companies/{company_id}/pay_periods/unprocessed_termination_pay_periods",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="get-v1-companies-company_id-unprocessed_termination_pay_periods",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                List[models.UnprocessedTerminationPayPeriod], http_res
            )
        if utils.match_response(http_res, ["404", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    async def get_unprocessed_termination_periods_async(
        self,
        *,
        company_id: str,
        x_gusto_api_version: Optional[
            models.VersionHeader
        ] = models.VersionHeader.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> List[models.UnprocessedTerminationPayPeriod]:
        r"""Get termination pay periods for a company

        When a payroll admin terminates an employee and selects \"Dismissal Payroll\" as the employee's final payroll, their last pay period will appear on the list.

        This endpoint returns the unprocessed pay periods for past and future terminated employees in a given company.

        scope: `payrolls:read`

        :param company_id: The UUID of the company
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetV1CompaniesCompanyIDUnprocessedTerminationPayPeriodsRequest(
            company_id=company_id,
            x_gusto_api_version=x_gusto_api_version,
        )

        req = self._build_request_async(
            method="GET",
            path="/v1/companies/{company_id}/pay_periods/unprocessed_termination_pay_periods",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="get-v1-companies-company_id-unprocessed_termination_pay_periods",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                List[models.UnprocessedTerminationPayPeriod], http_res
            )
        if utils.match_response(http_res, ["404", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    def get_assignments(
        self,
        *,
        company_id: str,
        x_gusto_api_version: Optional[
            models.VersionHeader
        ] = models.VersionHeader.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.PayScheduleAssignment:
        r"""Get pay schedule assignments for a company

        This endpoint returns the current pay schedule assignment for a company, with pay schedule and employee/department mappings depending on the pay schedule type.

        scope: `pay_schedules:read`

        :param company_id: The UUID of the company
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetV1CompaniesCompanyIDPaySchedulesAssignmentsRequest(
            company_id=company_id,
            x_gusto_api_version=x_gusto_api_version,
        )

        req = self._build_request(
            method="GET",
            path="/v1/companies/{company_id}/pay_schedules/assignments",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="get-v1-companies-company_id-pay_schedules-assignments",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.PayScheduleAssignment, http_res)
        if utils.match_response(http_res, ["404", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    async def get_assignments_async(
        self,
        *,
        company_id: str,
        x_gusto_api_version: Optional[
            models.VersionHeader
        ] = models.VersionHeader.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.PayScheduleAssignment:
        r"""Get pay schedule assignments for a company

        This endpoint returns the current pay schedule assignment for a company, with pay schedule and employee/department mappings depending on the pay schedule type.

        scope: `pay_schedules:read`

        :param company_id: The UUID of the company
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetV1CompaniesCompanyIDPaySchedulesAssignmentsRequest(
            company_id=company_id,
            x_gusto_api_version=x_gusto_api_version,
        )

        req = self._build_request_async(
            method="GET",
            path="/v1/companies/{company_id}/pay_schedules/assignments",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="get-v1-companies-company_id-pay_schedules-assignments",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["404", "4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.PayScheduleAssignment, http_res)
        if utils.match_response(http_res, ["404", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    def preview_assignment(
        self,
        *,
        company_id: str,
        type_: Nullable[models.PayScheduleAssignmentBodyType],
        x_gusto_api_version: Optional[
            models.VersionHeader
        ] = models.VersionHeader.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        hourly_pay_schedule_uuid: Optional[str] = None,
        salaried_pay_schedule_uuid: Optional[str] = None,
        default_pay_schedule_uuid: Optional[str] = None,
        partial_assignment: Optional[bool] = None,
        employees: Optional[
            Union[List[models.EmployeesModel], List[models.EmployeesModelTypedDict]]
        ] = None,
        departments: Optional[
            Union[List[models.DepartmentsModel], List[models.DepartmentsModelTypedDict]]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.PayScheduleAssignmentPreview:
        r"""Preview pay schedule assignments for a company

        This endpoint returns the employee changes, including pay period and transition pay periods, for changing the pay schedule.

        scope: `pay_schedules:write`

        :param company_id: The UUID of the company
        :param type: The pay schedule assignment type.
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param hourly_pay_schedule_uuid: Pay schedule for hourly employees.
        :param salaried_pay_schedule_uuid: Pay schedule for salaried employees.
        :param default_pay_schedule_uuid: Default pay schedule for employees.
        :param partial_assignment: Indicates whether the request provides pay schedule assignments for a partial list of employees or departments of the company. By default, this is set to false.
        :param employees: List of employees and their pay schedules.
        :param departments: List of departments and their pay schedules.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.PostV1CompaniesCompanyIDPaySchedulesAssignmentPreviewRequest(
            company_id=company_id,
            x_gusto_api_version=x_gusto_api_version,
            pay_schedule_assignment_body=models.PayScheduleAssignmentBody(
                type=type_,
                hourly_pay_schedule_uuid=hourly_pay_schedule_uuid,
                salaried_pay_schedule_uuid=salaried_pay_schedule_uuid,
                default_pay_schedule_uuid=default_pay_schedule_uuid,
                partial_assignment=partial_assignment,
                employees=utils.get_pydantic_model(
                    employees, Optional[List[models.EmployeesModel]]
                ),
                departments=utils.get_pydantic_model(
                    departments, Optional[List[models.DepartmentsModel]]
                ),
            ),
        )

        req = self._build_request(
            method="POST",
            path="/v1/companies/{company_id}/pay_schedules/assignment_preview",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.pay_schedule_assignment_body,
                False,
                False,
                "json",
                models.PayScheduleAssignmentBody,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="post-v1-companies-company_id-pay_schedules-assignment_preview",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["404", "422", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.PayScheduleAssignmentPreview, http_res
            )
        if utils.match_response(http_res, "422", "application/json"):
            response_data = unmarshal_json_response(
                models.UnprocessableEntityErrorObjectData, http_res
            )
            raise models.UnprocessableEntityErrorObject(response_data, http_res)
        if utils.match_response(http_res, ["404", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    async def preview_assignment_async(
        self,
        *,
        company_id: str,
        type_: Nullable[models.PayScheduleAssignmentBodyType],
        x_gusto_api_version: Optional[
            models.VersionHeader
        ] = models.VersionHeader.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        hourly_pay_schedule_uuid: Optional[str] = None,
        salaried_pay_schedule_uuid: Optional[str] = None,
        default_pay_schedule_uuid: Optional[str] = None,
        partial_assignment: Optional[bool] = None,
        employees: Optional[
            Union[List[models.EmployeesModel], List[models.EmployeesModelTypedDict]]
        ] = None,
        departments: Optional[
            Union[List[models.DepartmentsModel], List[models.DepartmentsModelTypedDict]]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.PayScheduleAssignmentPreview:
        r"""Preview pay schedule assignments for a company

        This endpoint returns the employee changes, including pay period and transition pay periods, for changing the pay schedule.

        scope: `pay_schedules:write`

        :param company_id: The UUID of the company
        :param type: The pay schedule assignment type.
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param hourly_pay_schedule_uuid: Pay schedule for hourly employees.
        :param salaried_pay_schedule_uuid: Pay schedule for salaried employees.
        :param default_pay_schedule_uuid: Default pay schedule for employees.
        :param partial_assignment: Indicates whether the request provides pay schedule assignments for a partial list of employees or departments of the company. By default, this is set to false.
        :param employees: List of employees and their pay schedules.
        :param departments: List of departments and their pay schedules.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.PostV1CompaniesCompanyIDPaySchedulesAssignmentPreviewRequest(
            company_id=company_id,
            x_gusto_api_version=x_gusto_api_version,
            pay_schedule_assignment_body=models.PayScheduleAssignmentBody(
                type=type_,
                hourly_pay_schedule_uuid=hourly_pay_schedule_uuid,
                salaried_pay_schedule_uuid=salaried_pay_schedule_uuid,
                default_pay_schedule_uuid=default_pay_schedule_uuid,
                partial_assignment=partial_assignment,
                employees=utils.get_pydantic_model(
                    employees, Optional[List[models.EmployeesModel]]
                ),
                departments=utils.get_pydantic_model(
                    departments, Optional[List[models.DepartmentsModel]]
                ),
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/v1/companies/{company_id}/pay_schedules/assignment_preview",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.pay_schedule_assignment_body,
                False,
                False,
                "json",
                models.PayScheduleAssignmentBody,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="post-v1-companies-company_id-pay_schedules-assignment_preview",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["404", "422", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.PayScheduleAssignmentPreview, http_res
            )
        if utils.match_response(http_res, "422", "application/json"):
            response_data = unmarshal_json_response(
                models.UnprocessableEntityErrorObjectData, http_res
            )
            raise models.UnprocessableEntityErrorObject(response_data, http_res)
        if utils.match_response(http_res, ["404", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    def assign(
        self,
        *,
        company_id: str,
        type_: Nullable[models.PayScheduleAssignmentBodyType],
        x_gusto_api_version: Optional[
            models.VersionHeader
        ] = models.VersionHeader.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        hourly_pay_schedule_uuid: Optional[str] = None,
        salaried_pay_schedule_uuid: Optional[str] = None,
        default_pay_schedule_uuid: Optional[str] = None,
        partial_assignment: Optional[bool] = None,
        employees: Optional[
            Union[List[models.EmployeesModel], List[models.EmployeesModelTypedDict]]
        ] = None,
        departments: Optional[
            Union[List[models.DepartmentsModel], List[models.DepartmentsModelTypedDict]]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ):
        r"""Assign pay schedules for a company

        This endpoint assigns employees to pay schedules based on the schedule type.
        For `by_employee` and `by_department` schedules, use the `partial_assignment` parameter to control the assignment scope. Set it to `true` for partial assignments (only some employees or departments at a time) and `false` for full assignments (all employees or departments at once).

        scope: `pay_schedules:write`

        :param company_id: The UUID of the company
        :param type: The pay schedule assignment type.
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param hourly_pay_schedule_uuid: Pay schedule for hourly employees.
        :param salaried_pay_schedule_uuid: Pay schedule for salaried employees.
        :param default_pay_schedule_uuid: Default pay schedule for employees.
        :param partial_assignment: Indicates whether the request provides pay schedule assignments for a partial list of employees or departments of the company. By default, this is set to false.
        :param employees: List of employees and their pay schedules.
        :param departments: List of departments and their pay schedules.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.PostV1CompaniesCompanyIDPaySchedulesAssignRequest(
            company_id=company_id,
            x_gusto_api_version=x_gusto_api_version,
            pay_schedule_assignment_body=models.PayScheduleAssignmentBody(
                type=type_,
                hourly_pay_schedule_uuid=hourly_pay_schedule_uuid,
                salaried_pay_schedule_uuid=salaried_pay_schedule_uuid,
                default_pay_schedule_uuid=default_pay_schedule_uuid,
                partial_assignment=partial_assignment,
                employees=utils.get_pydantic_model(
                    employees, Optional[List[models.EmployeesModel]]
                ),
                departments=utils.get_pydantic_model(
                    departments, Optional[List[models.DepartmentsModel]]
                ),
            ),
        )

        req = self._build_request(
            method="POST",
            path="/v1/companies/{company_id}/pay_schedules/assign",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.pay_schedule_assignment_body,
                False,
                False,
                "json",
                models.PayScheduleAssignmentBody,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="post-v1-companies-company_id-pay_schedules-assign",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["404", "422", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "*"):
            return
        if utils.match_response(http_res, "422", "application/json"):
            response_data = unmarshal_json_response(
                models.UnprocessableEntityErrorObjectData, http_res
            )
            raise models.UnprocessableEntityErrorObject(response_data, http_res)
        if utils.match_response(http_res, ["404", "4XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    async def assign_async(
        self,
        *,
        company_id: str,
        type_: Nullable[models.PayScheduleAssignmentBodyType],
        x_gusto_api_version: Optional[
            models.VersionHeader
        ] = models.VersionHeader.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        hourly_pay_schedule_uuid: Optional[str] = None,
        salaried_pay_schedule_uuid: Optional[str] = None,
        default_pay_schedule_uuid: Optional[str] = None,
        partial_assignment: Optional[bool] = None,
        employees: Optional[
            Union[List[models.EmployeesModel], List[models.EmployeesModelTypedDict]]
        ] = None,
        departments: Optional[
            Union[List[models.DepartmentsModel], List[models.DepartmentsModelTypedDict]]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ):
        r"""Assign pay schedules for a company

        This endpoint assigns employees to pay schedules based on the schedule type.
        For `by_employee` and `by_department` schedules, use the `partial_assignment` parameter to control the assignment scope. Set it to `true` for partial assignments (only some employees or departments at a time) and `false` for full assignments (all employees or departments at once).

        scope: `pay_schedules:write`

        :param company_id: The UUID of the company
        :param type: The pay schedule assignment type.
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param hourly_pay_schedule_uuid: Pay schedule for hourly employees.
        :param salaried_pay_schedule_uuid: Pay schedule for salaried employees.
        :param default_pay_schedule_uuid: Default pay schedule for employees.
        :param partial_assignment: Indicates whether the request provides pay schedule assignments for a partial list of employees or departments of the company. By default, this is set to false.
        :param employees: List of employees and their pay schedules.
        :param departments: List of departments and their pay schedules.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.PostV1CompaniesCompanyIDPaySchedulesAssignRequest(
            company_id=company_id,
            x_gusto_api_version=x_gusto_api_version,
            pay_schedule_assignment_body=models.PayScheduleAssignmentBody(
                type=type_,
                hourly_pay_schedule_uuid=hourly_pay_schedule_uuid,
                salaried_pay_schedule_uuid=salaried_pay_schedule_uuid,
                default_pay_schedule_uuid=default_pay_schedule_uuid,
                partial_assignment=partial_assignment,
                employees=utils.get_pydantic_model(
                    employees, Optional[List[models.EmployeesModel]]
                ),
                departments=utils.get_pydantic_model(
                    departments, Optional[List[models.DepartmentsModel]]
                ),
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/v1/companies/{company_id}/pay_schedules/assign",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.pay_schedule_assignment_body,
                False,
                False,
                "json",
                models.PayScheduleAssignmentBody,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="post-v1-companies-company_id-pay_schedules-assign",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            error_status_codes=["404", "422", "4XX", "5XX"],
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "*"):
            return
        if utils.match_response(http_res, "422", "application/json"):
            response_data = unmarshal_json_response(
                models.UnprocessableEntityErrorObjectData, http_res
            )
            raise models.UnprocessableEntityErrorObject(response_data, http_res)
        if utils.match_response(http_res, ["404", "4XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)
