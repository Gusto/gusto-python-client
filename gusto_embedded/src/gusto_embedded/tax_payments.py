"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from datetime import date
from gusto_embedded import models, utils
from gusto_embedded._hooks import HookContext
from gusto_embedded.types import OptionalNullable, UNSET
from gusto_embedded.utils import get_security_from_env
from gusto_embedded.utils.unmarshal_json_response import unmarshal_json_response
from typing import Any, Iterable, List, Mapping, Optional


class TaxPayments(BaseSDK):
    def get_tax_payments(
        self,
        *,
        company_uuid: str,
        x_gusto_api_version: Optional[
            models.GetTaxPaymentsHeaderXGustoAPIVersion
        ] = models.GetTaxPaymentsHeaderXGustoAPIVersion.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        jurisdiction: Optional[Iterable[str]] = None,
        payroll_uuids: Optional[Iterable[str]] = None,
        due_date_from: Optional[date] = None,
        due_date_to: Optional[date] = None,
        payment_sent_on_from: Optional[date] = None,
        payment_sent_on_to: Optional[date] = None,
        period_start_from: Optional[date] = None,
        period_start_to: Optional[date] = None,
        period_end_from: Optional[date] = None,
        period_end_to: Optional[date] = None,
        amount_min: Optional[float] = None,
        amount_max: Optional[float] = None,
        sort_by: Optional[str] = None,
        page: Optional[int] = None,
        per: Optional[int] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> List[models.TaxPayment]:
        r"""Get all tax payments for a company

        Fetches all tax payments Gusto has made (or scheduled) to tax agencies on behalf of a company.

        scope: `tax_payments:read`

        If set, this operation will use `company_access_auth` from the global security.

        :param company_uuid: The UUID of the company
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param jurisdiction: Filter by tax jurisdiction. Accepts one or more two-letter state codes, or `US` for federal, as a comma-separated list (e.g. `IL,CA`). Codes are case-insensitive.
        :param payroll_uuids: Filter to tax payments associated with one or more payrolls, given as a comma-separated list of payroll UUIDs.
        :param due_date_from: Lower-bound (inclusive) date filter on due_date.
        :param due_date_to: Upper-bound (inclusive) date filter on due_date.
        :param payment_sent_on_from: Lower-bound (inclusive) date filter on payment_sent_on.
        :param payment_sent_on_to: Upper-bound (inclusive) date filter on payment_sent_on.
        :param period_start_from: Lower-bound (inclusive) date filter on period_start.
        :param period_start_to: Upper-bound (inclusive) date filter on period_start.
        :param period_end_from: Lower-bound (inclusive) date filter on period_end.
        :param period_end_to: Upper-bound (inclusive) date filter on period_end.
        :param amount_min: Lower-bound (inclusive) filter on amount.
        :param amount_max: Upper-bound (inclusive) filter on amount.
        :param sort_by: Sort by one or more fields. Options: amount, due_date, payment_sent_on, period_end, period_start. Append `:asc` or `:desc` to specify direction (e.g., `amount:asc`). Defaults to ascending.
        :param page: The page that is requested. When unspecified, will load all objects unless endpoint forces pagination.
        :param per: Number of objects per page. For majority of endpoints will default to 25
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetTaxPaymentsRequest(
            x_gusto_api_version=x_gusto_api_version,
            company_uuid=company_uuid,
            jurisdiction=utils.unmarshal(jurisdiction, Optional[List[str]]),
            payroll_uuids=utils.unmarshal(payroll_uuids, Optional[List[str]]),
            due_date_from=due_date_from,
            due_date_to=due_date_to,
            payment_sent_on_from=payment_sent_on_from,
            payment_sent_on_to=payment_sent_on_to,
            period_start_from=period_start_from,
            period_start_to=period_start_to,
            period_end_from=period_end_from,
            period_end_to=period_end_to,
            amount_min=amount_min,
            amount_max=amount_max,
            sort_by=sort_by,
            page=page,
            per=per,
        )

        req = self._build_request(
            method="GET",
            path="/v1/companies/{company_uuid}/tax_payments",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            allowed_fields=["company_access_auth"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="get-tax-payments",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Tax Payments"],
                extensions={
                    "x-gusto-integration-type": ["embedded"],
                    "x-gusto-rswag": True,
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(List[models.TaxPayment], http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                models.NotFoundErrorObjectData, http_res
            )
            raise models.NotFoundErrorObject(response_data, http_res)
        if utils.match_response(http_res, "422", "application/json"):
            response_data = unmarshal_json_response(
                models.UnprocessableEntityError1Data, http_res
            )
            raise models.UnprocessableEntityError1(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    async def get_tax_payments_async(
        self,
        *,
        company_uuid: str,
        x_gusto_api_version: Optional[
            models.GetTaxPaymentsHeaderXGustoAPIVersion
        ] = models.GetTaxPaymentsHeaderXGustoAPIVersion.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        jurisdiction: Optional[Iterable[str]] = None,
        payroll_uuids: Optional[Iterable[str]] = None,
        due_date_from: Optional[date] = None,
        due_date_to: Optional[date] = None,
        payment_sent_on_from: Optional[date] = None,
        payment_sent_on_to: Optional[date] = None,
        period_start_from: Optional[date] = None,
        period_start_to: Optional[date] = None,
        period_end_from: Optional[date] = None,
        period_end_to: Optional[date] = None,
        amount_min: Optional[float] = None,
        amount_max: Optional[float] = None,
        sort_by: Optional[str] = None,
        page: Optional[int] = None,
        per: Optional[int] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> List[models.TaxPayment]:
        r"""Get all tax payments for a company

        Fetches all tax payments Gusto has made (or scheduled) to tax agencies on behalf of a company.

        scope: `tax_payments:read`

        If set, this operation will use `company_access_auth` from the global security.

        :param company_uuid: The UUID of the company
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param jurisdiction: Filter by tax jurisdiction. Accepts one or more two-letter state codes, or `US` for federal, as a comma-separated list (e.g. `IL,CA`). Codes are case-insensitive.
        :param payroll_uuids: Filter to tax payments associated with one or more payrolls, given as a comma-separated list of payroll UUIDs.
        :param due_date_from: Lower-bound (inclusive) date filter on due_date.
        :param due_date_to: Upper-bound (inclusive) date filter on due_date.
        :param payment_sent_on_from: Lower-bound (inclusive) date filter on payment_sent_on.
        :param payment_sent_on_to: Upper-bound (inclusive) date filter on payment_sent_on.
        :param period_start_from: Lower-bound (inclusive) date filter on period_start.
        :param period_start_to: Upper-bound (inclusive) date filter on period_start.
        :param period_end_from: Lower-bound (inclusive) date filter on period_end.
        :param period_end_to: Upper-bound (inclusive) date filter on period_end.
        :param amount_min: Lower-bound (inclusive) filter on amount.
        :param amount_max: Upper-bound (inclusive) filter on amount.
        :param sort_by: Sort by one or more fields. Options: amount, due_date, payment_sent_on, period_end, period_start. Append `:asc` or `:desc` to specify direction (e.g., `amount:asc`). Defaults to ascending.
        :param page: The page that is requested. When unspecified, will load all objects unless endpoint forces pagination.
        :param per: Number of objects per page. For majority of endpoints will default to 25
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetTaxPaymentsRequest(
            x_gusto_api_version=x_gusto_api_version,
            company_uuid=company_uuid,
            jurisdiction=utils.unmarshal(jurisdiction, Optional[List[str]]),
            payroll_uuids=utils.unmarshal(payroll_uuids, Optional[List[str]]),
            due_date_from=due_date_from,
            due_date_to=due_date_to,
            payment_sent_on_from=payment_sent_on_from,
            payment_sent_on_to=payment_sent_on_to,
            period_start_from=period_start_from,
            period_start_to=period_start_to,
            period_end_from=period_end_from,
            period_end_to=period_end_to,
            amount_min=amount_min,
            amount_max=amount_max,
            sort_by=sort_by,
            page=page,
            per=per,
        )

        req = self._build_request_async(
            method="GET",
            path="/v1/companies/{company_uuid}/tax_payments",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            allowed_fields=["company_access_auth"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="get-tax-payments",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Tax Payments"],
                extensions={
                    "x-gusto-integration-type": ["embedded"],
                    "x-gusto-rswag": True,
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(List[models.TaxPayment], http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                models.NotFoundErrorObjectData, http_res
            )
            raise models.NotFoundErrorObject(response_data, http_res)
        if utils.match_response(http_res, "422", "application/json"):
            response_data = unmarshal_json_response(
                models.UnprocessableEntityError1Data, http_res
            )
            raise models.UnprocessableEntityError1(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    def get_tax_payment(
        self,
        *,
        company_uuid: str,
        uuid: str,
        x_gusto_api_version: Optional[
            models.GetTaxPaymentHeaderXGustoAPIVersion
        ] = models.GetTaxPaymentHeaderXGustoAPIVersion.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.TaxPayment:
        r"""Get a tax payment for a company

        Fetches a single tax payment by UUID, including the payroll tax liabilities that make up the payment.

        scope: `tax_payments:read`

        If set, this operation will use `company_access_auth` from the global security.

        :param company_uuid: The UUID of the company
        :param uuid: The UUID of the tax payment
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetTaxPaymentRequest(
            x_gusto_api_version=x_gusto_api_version,
            company_uuid=company_uuid,
            uuid=uuid,
        )

        req = self._build_request(
            method="GET",
            path="/v1/companies/{company_uuid}/tax_payments/{uuid}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            allowed_fields=["company_access_auth"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="get-tax-payment",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Tax Payments"],
                extensions={
                    "x-gusto-integration-type": ["embedded"],
                    "x-gusto-rswag": True,
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.TaxPayment, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                models.NotFoundErrorObjectData, http_res
            )
            raise models.NotFoundErrorObject(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    async def get_tax_payment_async(
        self,
        *,
        company_uuid: str,
        uuid: str,
        x_gusto_api_version: Optional[
            models.GetTaxPaymentHeaderXGustoAPIVersion
        ] = models.GetTaxPaymentHeaderXGustoAPIVersion.TWO_THOUSAND_AND_TWENTY_FIVE_MINUS_06_MINUS_15,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.TaxPayment:
        r"""Get a tax payment for a company

        Fetches a single tax payment by UUID, including the payroll tax liabilities that make up the payment.

        scope: `tax_payments:read`

        If set, this operation will use `company_access_auth` from the global security.

        :param company_uuid: The UUID of the company
        :param uuid: The UUID of the tax payment
        :param x_gusto_api_version: Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.GetTaxPaymentRequest(
            x_gusto_api_version=x_gusto_api_version,
            company_uuid=company_uuid,
            uuid=uuid,
        )

        req = self._build_request_async(
            method="GET",
            path="/v1/companies/{company_uuid}/tax_payments/{uuid}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            allowed_fields=["company_access_auth"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="get-tax-payment",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Tax Payments"],
                extensions={
                    "x-gusto-integration-type": ["embedded"],
                    "x-gusto-rswag": True,
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.TaxPayment, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                models.NotFoundErrorObjectData, http_res
            )
            raise models.NotFoundErrorObject(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)
